# Findings ledger — 002-adaptive-local-model-serving — 2026-09-05

Extracted by task F1 (forensic extraction, read-only). Sources:
`specs/002-adaptive-local-model-serving/progress.yml` (4001 lines, 246 KB) and
`specs/002-adaptive-local-model-serving/tasks.md` (277 lines).

Method notes (anti-bluff, §11.4.6):

- "Open" = finding whose progress.yml status is not closed/resolved/fixed/landed/corrected/verified. This intentionally includes `recorded` rows (measured facts and method notes) — they are marked **note** in the table and are not burn-down work.
- Every file:line below was read in progress.yml; the key helix_llm anchors
  (entry.go:19/30, config.go:47, openai.go, choose.go:265, imagegen/videogen-boot
  Decide call sites, server.go:247) were additionally verified against the live
  trees on 2026-09-05.
- The verbatim T055 round-1 export-review report is **not stored as a standalone
  file** anywhere under `specs/002-adaptive-local-model-serving/`,
  `.superpowers/`, `docs/`, or `submodules/helix_llm/` (repo-wide grep). The
  T055-1..7 enumeration below is **reconstructed from cross-references inside
  progress.yml** and is marked UNCONFIRMED where the mapping is inferred.
- repo is `helix_llm` unless the finding itself names another repo.

---

## Step 1 — Open findings (status != closed/resolved): 54

Severity counts: **3 CRITICAL · 11 HIGH · 19 MEDIUM · 12 LOW · 9 INFO(note)**.
Defect work (excluding INFO notes): 45.

| id | sev | repo | file:line | defect (one line) | status | blocking |
|---|---|---|---|---|---|---|
| EX-3 | CRITICAL | helix_llm | internal/catalogue/entry.go:19,30 + data/speech_audio.yaml:1128,1241,1131 | Three audio-classification entries still commented out — blocked on unsourced measured figures, not the (now-existing) constants; stale comment at speech_audio.yaml:1131 | open — narrowed | data measurement; W2a-4 |
| CRITICAL-2 | CRITICAL | helix_llm | internal/runtime/choose.go:265 vs data/text.yaml:369,304 | ResidentMemoryFraction (0.25) discounts a streaming row's memory figure, which is the runtime's FLOOR — a 4 GiB host is admitted for a 16 GiB-minimum model; same field double-read as in-memory requirement | open — design decision | operator decision (catalogue+selection+runtime) |
| CRITICAL-4 | CRITICAL | helix_llm | services/videogen/ + internal/catalogue/data/video.yaml | Videogen has no GGUF load path at all yet gguf-q4 was servable; contained at precision layer — residual: wan2.2-t2v-a14b source still passes the allowlist gate pointing at the unquantised repo | contained — data decision outstanding | operator decision; W2a-4 |
| EX-1 | HIGH | helix_llm | services/videogen/.gitignore-meta/wan_ltx_gguf.yaml | Manifest declares ~18 GiB expected disk usage; measured 31.85 GiB (UMT5-XXL encoder) — provisioned hosts run out mid-fetch | recorded | W2a-4 |
| EX-2 | HIGH | helix_llm | services/videogen/videogen_server.py | `ltx` backend points at Lightricks/LTX-Video which has no .gguf; from_pretrained silently serves a ~2B diffusers transformer, not the documented 13B | recorded | W2a-4 |
| EX-5 | HIGH | helix_llm | internal/gateway/openai.go:33 (historic; list now removed — comment at :524) | hardcodedModels fabricated 3-entry list (gpt-4o, claude-sonnet-4 as owned_by helix) served when no Brain configured — **already fixed in tree (ab34fa8) but ledger status still `recorded`** | recorded (stale — see anomalies) | ledger update only |
| EX-22 | HIGH | helix_code | config/config.yaml `llm:` vs internal/config LLMConfig | Four keys (max_retries, timeout, providers, selection) read by nothing; an ignored timeout is a false operator belief; default_model never set by YAML | investigated — operator decision | operator decision; W2c-1 |
| EX-28 | HIGH | helix_llm | cmd/imagegen-boot/main.go:103 + cmd/videogen-boot/main.go:119 | Empty WeightsDir → storage axis measured on the working directory, not the volume that will hold weights (73–77x different answers); no test covers the axis in either lane | open — re-verified, widened | W2a-4 |
| CHALLENGES-triage | HIGH | helix_llm | challenges/ banks vs live server | 83 challenge failures triaged into 8 root-cause groups; G4 (null-data listing) fixed+guarded; remaining group counts need a suite re-run to re-measure | open — narrowed | G-sweep re-run; W2a-4 |
| OPEN-24 | HIGH | helix_llm | internal/catalogue/data (text.yaml) | Three former lane candidates (Mistral-Nemo-2407, GLM-4.7-Flash, DeepSeek-Coder-V2-Lite) absent from the catalogue — operators with working GGUFs are now refused | open — operator decision | operator decision |
| SECURITY-5 | HIGH | helix_llm | docs/manual/security.md + docs/manual/modules.md | Security manual documented JWT auth that does not exist — docs fixed (NOT IMPLEMENTED struck through); whether to implement JWT or drop the field is undecided | documentation fixed; decision open | operator decision |
| SECURITY-6 | HIGH | helix_llm | docker-compose.mcp-full.yml | Compose still launches three npm package names held for security even though the Go catalogues are fixed | open — narrowed | W2a-4 |
| HXC-prodconfig | HIGH | helix_code | config/production-config.yaml | 22 keys (environments, features, performance, security sections) configured at length and read by nothing — registered as inert, await operator decision | fixed to unambiguous extent; decision open | operator decision; W2c-1 |
| ROOT-1 | HIGH | claude_toolkit | scripts/providers/helixagent.json (18434/v1), helixagent-native.json (18435), helixllm-gateway.json (18435/v1), claude-providers.sh:400,428,2084, lib.sh:2189 | Hardcoded provider base_urls point at dead ports; :18434 now answers as llama-server — a LIVE BUT WRONG service that will connect and mis-serve | open — narrowed | W2d-1 + T052/T053 |
| OPEN-1 | MEDIUM | helix_llm | internal/gateway HandleListModels | Withheld models cannot ride the OpenAI-compatible /v1/models without breaking client assumptions; only the serving half is emitted (FR-019 half-met on HTTP) | open — design decision | design decision; W2a-4 |
| OPEN-5 | MEDIUM | helix_llm | data/text.yaml (5 of 6 entries) → selection/vrambroker | requires_accelerator:false entries skip the device axis and hand a host-RAM figure to the VRAM broker — the CRITICAL-5 shape on the RAM/VRAM axis | open | own change; W2a-4 |
| SELF-6 | MEDIUM | helix_llm | n/a — plan.md:175 | T070 option framing implied a go.mod change the plan itself already recorded as impossible (C runtime, no binding) | recorded (note) | n/a |
| SELF-8 | MEDIUM | helix_llm | commit 064dd75 | git add -A swept another agent's seven harness files into a 46-file commit; recovery (reset --soft + re-stage) is an operator decision | recorded — operator decision | operator decision |
| OPEN-21 | MEDIUM | helix_llm | internal/selection/placement.go (NewFleet) | Fleet has no production caller — per-device commitment accounting is correct but unexercised; future callers must pass runtime.SelectionReserve() | open — narrowed | W2a-4 when wired |
| SELF-12 | MEDIUM | helix_llm | n/a — process | Operator decided the empty-secret policy on a false premise the agent then disproved; correct rule shipped instead | recorded (note) | n/a |
| OPEN-22 | MEDIUM | helix_llm | Auth.JWTSecret (config) | Field declared, configured, documented — and read by nothing; never built (arrived 48a6479). SECURITY-5 documented it | open — investigate before remove/wire | operator decision (with SECURITY-5) |
| OPEN-26 | MEDIUM | helix_llm | cmd/videogen-boot/modelchoice.go (4th copy) | Lane model-choice logic byte-identical across four lanes (videogen, imagegen, visiongen, agentgen) — divergence risk at the worst point | open — deliberate deferral | quiet-checkout extraction; W2a-4 |
| SELF-13 | MEDIUM | helix_llm | commit 52c827f | Commit titled as a 32-line FAQ change contains 1424 lines of another agent's in-flight gateway work; not rewritten deliberately, record is complete | recorded (note) | n/a |
| HXA-testharness-1 | MEDIUM | helix_agent | tests/integration (5 tests) + bin/helixagent (gitignored) | Five integration tests require an unbuilt gitignored artifact; build-and-rerun verification blocked by another agent's in-flight compile error at internal/handlers/openai_compatible.go:1367 | open — narrowed | W2b pass; rerun when tree compiles |
| HXA-testharness-2 | MEDIUM | helix_agent | tests/integration/cli_schema_validation_test.go:166 | Test reads the operator's real ~/.config/opencode/opencode.json and Fatalf's when absent — asserts a fact about the operator's machine, not the code | open — needs intent decision | W2b pass |
| OPEN-29 | MEDIUM | helix_llm | internal/gateway/consumer_config.go:204 | MergeOpenCode replaces the user's opencode.json wholesale while the backend is loading — server-side-only data-loss exposure (toolkit fix does not cover it) | open — server-side | W2a-4 |
| TOOLKIT-uncommitted | MEDIUM | claude_toolkit | branch fix/helixllm-export-review-findings | Whole helixllm-export feature + its fixes live uncommitted on a branch; merging lands the feature, not just the fixes | fixed on branch; merge is operator decision | operator decision; W2d-1 |
| OPEN-33 | MEDIUM | helix_llm | data/vision_image.yaml | Five requires_accelerator:false entries whose figures were never re-examined for RAM-vs-VRAM sourcing; selection fix bounds the offer but the numbers' meaning is unverified | open | W2a-4 (with OPEN-5) |
| BOOT-4 | MEDIUM | helix_code | Dockerfile context vs go.mod replace directives | Container build cannot resolve helix_code's 43 replace directives pointing outside the copied directory; needs scoped .dockerignore + layout change | open | W2c-1 |
| TEST-2 | MEDIUM | helix_code | tests/memory (getTestBaseURL) | Whole suite silently skips when no server is up — reports "ok" having exercised nothing (§11.4.238 coverage gap); leak signal itself refuted on a quiet host | open — coverage gap only | W2c-1 |
| TOOLKIT-2 | MEDIUM | claude_toolkit | scripts/tests/test_providers.sh | Semantic judge cannot run in this environment → skip/exit 2 → 11 downstream assertions fail behind one root cause | open — needs judge-capable host | W2d-1 |
| BOOT-8 | MEDIUM | helix_llm | internal/shared/config/config.go:47 | Hardcoded default Llama-3.1-70B-Instruct-Q4_K_M (~40 GB) on a 30 GB RAM / 12 GB VRAM host — offer-what-cannot-start in a default value; host now runs 3B via .env | open — narrowed to the default | W2a-4 |
| BOOT-9 | MEDIUM | helix_code | config/config.yaml `local:` (localhost:11434) + /api/v1/llm/providers | ollama disabled in config yet `local:` still points at dead :11434 and the providers endpoint lists ollama status:available from config, not a probe | open — one unprobed symptom | W2c-1 |
| EX-24 | LOW | helix_llm | deploy/compose.yaml + compose.dev.yaml | ${HELIX_LLM_LOCAL_MODEL:-model.gguf} placeholder fallback — fails loudly, a lesser class; recorded for completeness | recorded | W2a-4 |
| OPEN-16 | LOW | helix_code | go-gl/glfw (desktop GUI) | GUI build fails on missing X11 headers — environmental, pre-existing, recorded so it is not mistaken for a regression | open — environmental | none (document) |
| OPEN-20 | LOW | helix_llm | internal/discovery Send pinning | Behaviour change: a legitimately re-addressed instance is unreachable until re-discovered; Send has no redirect policy of its own (asymmetric, not a hole) | recorded — behaviour change | n/a |
| SELF-11 | LOW | helix_llm | n/a — process | Nearly misread my own grep-filtered output as a finding (PATTERN-1) | recorded (note) | n/a |
| OPEN-30 | LOW | helix_llm | internal/naming RetiredHosts | Retired set is exactly {localhost, 127.0.0.1}, deliberately; 0.0.0.0/::1 excluded until evidence they were ever published | open — deliberate | evidence-gated |
| OPEN-32 | LOW | n/a (hawkscan hook) | n/a — environmental | Hook announced a helix_llm commit SHA (1f97cf1) in a helix_agent session — cross-repo evidence mis-attribution | open — environmental | none (known) |
| OPEN-34 | LOW | helix_llm | cmd/helixllm (TestChatCompletions_HostWithNoListedModelsIsStillLive/host_127.0.0.1) | Intermittent failure under parallel full-suite runs; mechanism UNCONFIRMED (2s /health probe, load-sensitive); worth measuring before labelling | open — measure first | W2a-4 |
| METHOD-1 | LOW | cross-repo | n/a — method | Four concurrent suites manufactured false failures in three of them (timing, port exhaustion, live-server interference) — §11.4.119 single-owner violation | recorded (note) | n/a |
| OBS-1 | LOW | helix_code | internal/server/server.go:235-240,247 (+ handlers_test.go:743,808) | /api/v1/auth/me was never registered (it was a test-only route); the real endpoint is /api/v1/users/me — remaining question is which path to publish | open — narrowed | W2c-1 |
| TOOLKIT-1 | LOW | claude_toolkit | scripts/tests/test_claude.sh:7 | Test hardcodes the operator's live alias file, never sources lib/sandbox.sh — asserts on operator machine state, can pass on stale aliases | open — needs sandboxing | W2d-1 |
| TOOLKIT-3 | LOW | claude_toolkit | scripts/claude-providers.sh (_cma_helixllm_fetch_models) | Misleading "host unreachable" message for a reachable-but-untrusted (self-signed) host; works around cleanly with CURL_CA_BUNDLE | open — message is the finding | W2d-1 |
| SELF-15 | LOW | helix_llm | commit 29d8c87 message | Wrong cross-repo SHA in a commit body (be54764 vs real 8cb9b93a); correct pairing recorded here | recorded (note) | n/a |
| EX-10 | INFO | helix_llm | internal/catalogue/data/ (all four files) | All 30 shipped entries lack digests — Integrity.Complete() false catalogue-wide; "visible but not acquirable" is the universal state until first download | recorded (measured fact) | n/a |
| EX-19 | INFO | helix_llm | n/a — sweep methodology | Sweep disclosed tree drift and a make-build deviation — trustworthy-report note | recorded (note) | n/a |
| NOTE-2 | INFO | helix_llm | n/a — SECURITY-1 scopes | Reflective scan (helix_llm) vs explicit list (helix_code) are both correct — property of each codebase | recorded (note) | n/a |
| NOTE-3 | INFO | helix_llm | internal/lifecycle TestEvict_..._Concurrent | Lifecycle liveness assertion is genuinely flaky under CPU starvation (pre-existing) — deserves a ticket in its owning area | recorded (note) | owning-area ticket |
| NOTE-4 | INFO | helix_llm | n/a — review method | Three T055 severity claims failed verification; a review finding is a place to look, not a thing to implement | recorded (note) | n/a |
| NOTE-5 | INFO | cross-repo | n/a — label method | "environment-bound" label checked three times, wrong three times — proving a failure is not yours ends the inquiry too early | recorded (note) | n/a |
| NOTE-6 | INFO | helix_llm | n/a — T055 rounds | Round 1 found 7, round 2 found 4 (three created by round-1 fixes), round 3 found 2 — the iterate-to-GO argument | recorded (note) | n/a |
| NOTE-7 | INFO | helix_llm | loopback_serving_host_test.go | A skip whose condition is the defect's precondition is a blind spot aimed at the target | recorded (note) | n/a |
| MILESTONE-green | INFO | helix_llm | n/a — suite state | helix_llm fully green: 53 packages, 0 failures (point-in-time, quiet tree) | recorded | n/a |

Borderline rows excluded from "open" by the status rule but carrying residual work:
`CRITICAL-1-RESOLVED` (status FIXED — 83 real challenge failures await their own work;
covered by CHALLENGES-triage re-measure), `TOOLKIT-1-2-3` (fixed + guarded, commit pending
final suite), `PATTERN-1` (all three instances fixed), `SELF-1/2/9/10/16` (fixed/recorded).

---

## Step 2 — T055 export-review defects (7 critical/high) — all closed; enumeration UNCONFIRMED

The verbatim T055 round-1 report is not on disk (repo-wide grep: no standalone file; only
cross-references inside progress.yml). Round 1 = "found 7" (NOTE-6, progress.yml:2915);
the design doc (docs/superpowers/specs/2026-09-05-local-adaptive-serving-design.md:79)
says "7 critical/high from export review". Reconstruction below; severity-to-item mapping
is UNCONFIRMED for T055-2..7.

| id | sev (claimed) | repo | file:line | defect | outcome |
|---|---|---|---|---|---|
| T055-1 | CRITICAL | helix_llm | cmd/helixllm/main.go:461; internal/brain/brain.go:191,273 | Production HTTP path never resolves the published model identifier — fallback.Chain overwrites req.Model with each provider's FIRST model (client asking for qwen2.5:7b got llama3:8b) | CONFIRMED + fixed + guarded (Brain.PinModel; CRITICAL-7-resolved) |
| T055-2 | HIGH (UNCONFIRMED) | helix_llm | internal/naming derive/ServingHost (identity renderings) | Published identity named 127.0.0.1 — named no machine and collided across machines (two-hosts-collide) | Fixed (f63b96f line; OPEN-17-18-resolved) |
| T055-3 | HIGH (UNCONFIRMED) | helix_llm | internal/naming/export.go + HelixCode/OpenCode exporters | Export dead-code: exporters had no caller at all — declared, documented, unreachable | Fixed (OPEN-17/18-resolved) |
| T055-4 | HIGH | claude_toolkit | scripts/claude-providers.sh helixllm-export (exact line UNCONFIRMED) | "Corrupted identities": jq @tsv escaping claimed to break org/model and hf.co names | CONFIRMED but severity claim REFUTED — @tsv escapes only backslash/control chars; fixed narrower + guarded (TOOLKIT-1-2-3 FINDING 1) |
| T055-5 | HIGH | claude_toolkit | export path — discovery Bearer secret (exact line UNCONFIRMED) | "Misdirected credential": secret disclosed to every configured host; predicted 401s REFUTED — default is open access, so requests SUCCEEDED while leaking | CONFIRMED (worse, different way) + fixed + guarded (TOOLKIT-1-2-3 FINDING 2) |
| T055-6 | HIGH | claude_toolkit | scripts/claude-providers.sh --apply retirement (test lock-in at :474) | "Add-only --apply": a withdrawn model's .env and alias survived and stayed invocable | CONFIRMED + fixed + guarded with three-gate deletion safety; round-2 F2 found the fix fails open → F2-resolved |
| T055-7 | CRITICAL/HIGH (UNCONFIRMED) | helix_llm | naming/export design | OpenAI-compat local backends "silently dropped" | REFUTED — deliberate design; fixing it would stamp identities on proxied vendor models, which the design forbids; no change (NOTE-4) |

Related, reported LOW (outside the 7): withheld-vocabulary disagreement — REFUTED as a
false alarm; the real adjacent finding (wire field had no writer) is also closed
(brain.go:442 writer confirmed; five-key vocabulary agreement, T055-LOW-verdict).

Follow-on review rounds (recorded for completeness): round 2 = T055-REREVIEW-NOGO
F1/F2/F3/F4/W1 — all closed (F1-F4-W1-resolved, F2-resolved, WITHHELD-on-wire); round 3 =
T055-ROUND3-NOGO F-1/F-2 — both closed (F-1-resolved, F-2-resolved).

Note for W2a-3: the plan assumes T055-1..7 are unfixed deferrals ("write the failing test
… fix minimally"). The ledger shows every reconstructed T055 item is already fixed +
guarded or refuted. W2a-3 should therefore verify the existing guards (mutation-check)
rather than re-derive RED tests — controller decision.

---

## Step 3 — Open spec-002 tasks: 8 of 97 (89 done)

| task | scope | what remains |
|---|---|---|
| T037 [REVIEW][US1] | Independent review of the US1 diff (Fable xhigh; index-based binding; config-named models; offer terms) | review to zero-finding GO |
| T052 [US2] | Provider-alias synchronisation in claude_toolkit/scripts/claude-providers.sh + confirm the result (FR-035) | blocked by ROOT-1 wrong base_urls |
| T053 [US2] | Live-validate HelixAgent + HelixLLM answer through a synchronised alias (FR-034, SC-019) | needs T052 + a serving local model |
| T054 [REVIEW][US2] | Release new Claude Toolkit version with changelogs via GH/GL CLIs (FR-036/037) | operator checkpoint — outward publish; needs TOOLKIT-uncommitted merge decision |
| T055 [REVIEW][US2] | Independent review of the US2 diff (FR-014a) | 3 rounds done (7+5+2 findings, all closed) but the task checkbox is still unchecked — ledger/task sync gap |
| T068 [REVIEW][US3] | Security review of US3 diff — credentials, trust boundary, untrusted-instance observability | review to GO |
| T084 [REVIEW][US5] | Review of US5 diff — no family retaining a private static-selection path | review to GO |
| T097 [REVIEW] | Final cross-phase independent review to zero-finding GO | last |

---

## Step 4 — Machine-readable JSON

```json
[
  {"id": "EX-1", "repo": "helix_llm", "severity": "HIGH", "file": "services/videogen/.gitignore-meta/wan_ltx_gguf.yaml", "defect": "Manifest declares ~18 GiB expected disk usage; measured 31.85 GiB from the upstream blob API (UMT5-XXL encoder downloaded in full) — a host provisioned to the manifest runs out of disk mid-fetch.", "fix_direction": "Correct expected_disk_usage to the measured figure (re-measure at fix time) and record provenance per §11.4.77 meta.", "test_plan": "Assertion test summing live blob Content-Lengths for the declared path and comparing against the manifest figure; fails when understated."},
  {"id": "EX-2", "repo": "helix_llm", "severity": "HIGH", "file": "services/videogen/videogen_server.py", "defect": "ltx backend configured against Lightricks/LTX-Video, which contains no .gguf; from_pretrained resolves a ~2B diffusers transformer instead of the documented 13B checkpoints.", "fix_direction": "Point the ltx backend at a repo carrying real 13B GGUF builds, or keep ltx marked UNSUPPORTED until one is sourced (as the CRITICAL-4 containment did).", "test_plan": "RED: configure ltx without VIDEOGEN_MODEL override; assert the resolved checkpoint config is a 13B GGUF (48x32x128), not the 2B transformer; guard on the repo-id allowlist."},
  {"id": "EX-3", "repo": "helix_llm", "severity": "CRITICAL", "file": "internal/catalogue/entry.go:19,30 + data/speech_audio.yaml:1128,1241,1131", "defect": "Three audio-classification entries still commented out for want of measured runtime-memory and on-disk-weight figures; stale comment at speech_audio.yaml:1131 claims the family constant is missing (it exists at entry.go:30).", "fix_direction": "Measure whisper-class models on a host that has the GGUFs, record provenance-sourced figures, uncomment the three entries, delete the stale comment.", "test_plan": "Extend TestShippedCatalogueLoads to assert audio-classification entries are live once figures land; paired mutation re-commenting one entry must FAIL."},
  {"id": "EX-5", "repo": "helix_llm", "severity": "HIGH", "file": "internal/gateway/openai.go:33 (historic; removed — surviving comment at :524)", "defect": "hardcodedModels fabricated three-entry list (gpt-4o, claude-sonnet-4-20250514 as owned_by helix) returned whenever no Brain configured — BLUFF-002/CONST-036 false availability. ALREADY FIXED IN TREE (ab34fa8) though ledger status still says recorded.", "fix_direction": "None — verify the landed fix and update the ledger row; empty list carries a stated reason.", "test_plan": "Existing guard internal/gateway/listmodels_null_data_test.go plus listing-with-no-backend test asserting Data is empty-with-reason, never fabricated entries."},
  {"id": "EX-10", "repo": "helix_llm", "severity": "INFO", "file": "internal/catalogue/data/ (all four files)", "defect": "All 30 shipped entries carry integrity algorithm+size but no digest — Integrity.Complete() is false catalogue-wide; nothing shipped is acquirable until first download. Measured fact, correct behaviour.", "fix_direction": "Decide whether acquisition UX needs a digest-bootstrap step; document the universal not-yet-acquirable state (docs already do).", "test_plan": "Measured-fact row; re-measure the digest count if the catalogue changes. No defect test owed."},
  {"id": "EX-19", "repo": "helix_llm", "severity": "INFO", "file": "n/a — sweep methodology note (progress.yml:538)", "defect": "Sweep agent disclosed tree drift (13 files changed mid-run) and a make-build deviation — trustworthiness note, not a defect.", "fix_direction": "None — method note.", "test_plan": "n/a."},
  {"id": "EX-22", "repo": "helix_code", "severity": "HIGH", "file": "config/config.yaml (llm section) vs internal/config LLMConfig", "defect": "Four keys — max_retries, timeout, providers, selection — declared in config.yaml and read by nothing; an operator setting llm.timeout:30 has configured nothing and is told nothing. default_model never set by YAML.", "fix_direction": "Operator decision: honour the four keys, remove them, or make the loader refuse unknown keys (strict-loader, the option that would have prevented all four).", "test_plan": "RED: load config.yaml with llm.timeout set; assert the value is honoured or the loader refuses by name — today it decodes into nothing silently. Strict-loader variant asserts refusal."},
  {"id": "EX-24", "repo": "helix_llm", "severity": "LOW", "file": "deploy/compose.yaml + deploy/compose.dev.yaml", "defect": "${HELIX_LLM_LOCAL_MODEL:-model.gguf} placeholder fallback — fails loudly at load rather than silently substituting; a lesser defect class, recorded for completeness.", "fix_direction": "Replace the placeholder fallback with a required variable (fail at compose config time) or a documented quickstart model.", "test_plan": "compose config lint asserting no placeholder .gguf fallback, or a boot test asserting the loud failure names the missing variable."},
  {"id": "OPEN-1", "repo": "helix_llm", "severity": "MEDIUM", "file": "internal/gateway HandleListModels (/v1/models)", "defect": "FR-019 requires consumers to distinguish available from withheld models, but /v1/models is OpenAI-compatible where clients assume everything listed is usable; only the serving half (availability:serving + host) is emitted today.", "fix_direction": "Design decision: a richer FR-019 endpoint or an explicit opt-in query parameter carrying withheld entries — never silently stuffed into the compat listing.", "test_plan": "Contract test pinning current behaviour (serving entries carry availability+host; withheld omitted); the RED test for whichever design lands."},
  {"id": "CRITICAL-2", "repo": "helix_llm", "severity": "CRITICAL", "file": "internal/runtime/choose.go:265 vs internal/catalogue/data/text.yaml:369,304", "defect": "Choose discounts a streaming row's memory_required_bytes by defaultResidentMemoryFraction (0.25), but for runtime:streaming that figure IS the runtime's stated floor — a 4 GiB host is admitted for a 16 GiB-minimum model. The same field is also read as the in-memory requirement (two artifact shapes read one row).", "fix_direction": "Design decision spanning catalogue+selection+runtime: split GGUF and Colibri rows into separate entries with distinct figures (text.yaml:304 already documents two artifact shapes per model); reconciling the 12 fixtures that assume the discount per §11.4.120.", "test_plan": "RED: 4 GiB-free host + glm-5.2 streaming row must be refused; reconciled fixtures assert the new semantics; paired mutation restoring the discount must FAIL."},
  {"id": "SELF-6", "repo": "helix_llm", "severity": "MEDIUM", "file": "n/a — plan.md:175 (framing note)", "defect": "T070 adoption option was framed to the operator as a go.mod change that plan.md:175 already recorded as impossible (C runtime, no Go binding); operator chose on the wrong premise.", "fix_direction": "None — the landed integration (OS process + HTTP health probe, config-injected paths) is the correct outcome; note stands so the premise is on record.", "test_plan": "n/a."},
  {"id": "CRITICAL-4", "repo": "helix_llm", "severity": "CRITICAL", "file": "services/videogen/ + internal/catalogue/data/video.yaml", "defect": "Videogen has no GGUF load path at all (no from_single_file / GGUFQuantizationConfig), yet gguf-q4 was declared servable and wan2.2-t2v-a14b's source still passes the allowlist gate pointing at the unquantised repo (117.53 GiB fetch against an 8 GiB figure).", "fix_direction": "Data decision: record a source that genuinely serves GGUF-Q4 builds, or withdraw the source so boot refuses it as ltx now is.", "test_plan": "RED polarity: boot planning wan2.2-t2v-a14b must refuse with a recorded reason; guard asserting the source allowlist admits only GGUF-serving repos (RED_MODE=1 on the pre-fix data)."},
  {"id": "SELF-8", "repo": "helix_llm", "severity": "MEDIUM", "file": "commit 064dd75", "defect": "git add -A while another agent was verifying swept its seven harness files into a 46-file commit the operator wanted as its own commit.", "fix_direction": "Operator decision on history recovery (reset --soft + re-stage); adopt pathspec-limited commits (git commit -- <paths>) + git diff --cached --stat immediately before commit.", "test_plan": "n/a (process). House rule recorded in the ledger."},
  {"id": "OPEN-16", "repo": "helix_code", "severity": "LOW", "file": "go-gl/glfw (applications/desktop)", "defect": "Desktop GUI cannot build in this environment: X11/Xlib.h missing. Pre-existing, environmental; recorded so a later run does not mistake it for a regression.", "fix_direction": "Install X11 dev headers on build hosts or document the GUI build prerequisite.", "test_plan": "SKIP-with-reason on hosts lacking X11 headers, naming the missing prerequisite."},
  {"id": "NOTE-2", "repo": "helix_llm", "severity": "INFO", "file": "n/a — SECURITY-1 scope note (progress.yml:1306)", "defect": "helix_llm scans every string field reflectively; helix_code scans an explicit credential list only — both correct, a property of each codebase's expansion semantics.", "fix_direction": "None — do not 'fix' one into the other.", "test_plan": "n/a."},
  {"id": "OPEN-20", "repo": "helix_llm", "severity": "LOW", "file": "internal/discovery (Send pinning)", "defect": "Behaviour change from the Send pinning fix: an instance that legitimately changes address is unreachable until re-discovered; Send has no redirect policy of its own (asymmetric, not a hole).", "fix_direction": "Documented trade; optionally note in operator-facing discovery docs.", "test_plan": "n/a (documented behaviour change)."},
  {"id": "SELF-11", "repo": "helix_llm", "severity": "LOW", "file": "n/a — process note", "defect": "Nearly misread my own grep-filtered output ('0 packages ok') as a catastrophe — PATTERN-1 against the operator's own filter.", "fix_direction": "None — method note.", "test_plan": "n/a."},
  {"id": "NOTE-3", "repo": "helix_llm", "severity": "INFO", "file": "internal/lifecycle TestEvict_NeverUnloadsUnderAnInFlightRequest_Concurrent", "defect": "The liveness assertion is genuinely flaky under CPU starvation (reproduced deterministically at GOMAXPROCS=1, identical at the prior commit) — pre-existing, deserves a ticket in its owning area.", "fix_direction": "Fix the flaky liveness precondition in its own change in the lifecycle package.", "test_plan": "Reproduce under GOMAXPROCS=1; make the contention window deterministic or the assertion tolerant; paired mutation per §1.1."},
  {"id": "OPEN-21", "repo": "helix_llm", "severity": "MEDIUM", "file": "internal/selection/placement.go (NewFleet)", "defect": "Fleet has no production caller (grep-empty outside tests) — per-device commitment accounting is correct but unexercised; a future caller must pass runtime.SelectionReserve() or it reintroduces the over-commitment gap for its own path.", "fix_direction": "Wire a production caller through SelectionReserve() when one appears; until then leave the ledger correct-but-unexercised.", "test_plan": "grep-guard: any NewFleet( call outside tests must be accompanied by a SelectionReserve call-site test."},
  {"id": "SELF-12", "repo": "helix_llm", "severity": "MEDIUM", "file": "n/a — process note", "defect": "Operator decided the empty-secret policy on a false premise (empty JWT key 'arguably worse'); the agent disproved it (all credentials self-gating, JWT never consumed) and shipped the correct rule (non-blank once supplied).", "fix_direction": "None — correct rule landed; the narrowed decision is on record.", "test_plan": "n/a."},
  {"id": "OPEN-22", "repo": "helix_llm", "severity": "MEDIUM", "file": "Auth.JWTSecret (config; go.mod replace-only auth submodule)", "defect": "Field declared, configured, documented — and read by nothing; it was never built (arrived in 48a6479 with no implementation following). §11.4.124 investigation complete via SECURITY-5.", "fix_direction": "Operator decision (with SECURITY-5): implement JWT auth (the field is where it would land; blank-credential guard already covers it) or drop the field.", "test_plan": "If implemented: startup/401 round-trip tests + existing blank-credential guard. If dropped: config-refusal test asserting the field is rejected by name."},
  {"id": "EX-28", "repo": "helix_llm", "severity": "HIGH", "file": "cmd/imagegen-boot/main.go:103 + cmd/videogen-boot/main.go:119", "defect": "Empty WeightsDir at the laneboot.Decide call means the storage axis is measured on the working directory (os.Getwd fallback at measure_storage.go:49), not the named volume that will hold weights — 73-77x different answers on one host; nothing tests the axis in either lane.", "fix_direction": "Pass the real weights dir (podman named volume path) in both lanes; add a guard test per lane; scope the fix to both lanes, not imagegen alone.", "test_plan": "RED: plan from a small tmpfs cwd with the weights volume elsewhere must report the weights-volume figure; mutation restoring the empty string must FAIL. Add the videogen-lane variant."},
  {"id": "CHALLENGES-triage", "repo": "helix_llm", "severity": "HIGH", "file": "challenges/ banks vs live server (internal/gateway/openai.go:552 for G4)", "defect": "83 challenge failures classified into 8 root-cause groups (G1 environmental 51, G2a validation 11, G2b auth-config 3, G3 fixed 5, G4 fixed 4, G5 environmental 6, G6/G7 environmental 2, G8 undetermined 1); the remaining group counts are a point-in-time measurement needing a re-run to re-measure.", "fix_direction": "Re-run the suite and re-measure the group counts; then decide the G2a validation policy once for both handlers.", "test_plan": "Full challenge suite re-run against a live, loaded backend; per-group counts compared against this triage; no skip conversions (forbidden)."},
  {"id": "OPEN-24", "repo": "helix_llm", "severity": "HIGH", "file": "internal/catalogue/data (text.yaml)", "defect": "Three former lane candidates (Mistral-Nemo-Instruct-2407, GLM-4.7-Flash, DeepSeek-Coder-V2-Lite) are not in the catalogue — an operator with a working GGUF is now refused rather than served (the flip side of the FR-056 fix).", "fix_direction": "Operator decision: measure those GGUFs on a host that has them and add catalogue entries with recorded provenance, or accept the narrowed candidate set.", "test_plan": "Per-model catalogue entry tests with recorded provenance; boot plan asserting each model is offered when its measured footprint fits and refused with a reason when it does not."},
  {"id": "OPEN-25", "repo": "helix_llm", "severity": "MEDIUM", "file": "data/text.yaml (5 of 6 entries) → selection/vrambroker", "defect": "requires_accelerator:false entries skip the device axis entirely and hand a host-RAM measurement to vrambroker.Acquire — a number checked against one kind of memory spent against another (the CRITICAL-5 shape, narrower).", "fix_direction": "Own change: re-examine the five entries' figures for RAM-vs-VRAM sourcing and make selection check the figure against the kind of memory that will fund it.", "test_plan": "RED: offer a RAM-measured entry only when host RAM will fund it; paired mutation swapping the memory kind must change the verdict."},
  {"id": "OPEN-26", "repo": "helix_llm", "severity": "MEDIUM", "file": "cmd/videogen-boot/modelchoice.go (4th byte-identical copy)", "defect": "catalogueDir-through-refusalError logic is byte-identical across videogen, imagegen, visiongen and agentgen lanes — four copies is where a divergence becomes likely.", "fix_direction": "Extract the shared decision into one laneboot-owned helper when the checkout is quiet; per-lane thin adapters.", "test_plan": "Characterisation tests pinning all four lanes' current behaviour before extraction; after extraction one implementation + four adapters, mutation changing one lane's answer must FAIL."},
  {"id": "NOTE-4", "repo": "helix_llm", "severity": "INFO", "file": "n/a — review-method note (progress.yml:2042)", "defect": "Three T055 severity claims failed verification; a review finding is a place to look, not a thing to implement.", "fix_direction": "None — method note.", "test_plan": "n/a."},
  {"id": "OPEN-29", "repo": "helix_llm", "severity": "MEDIUM", "file": "internal/gateway/consumer_config.go:204 (MergeOpenCode)", "defect": "Server-side only: while the backend is loading, MergeOpenCode replaces the user's opencode.json wholesale — a real data-loss exposure the toolkit fix neither covers nor needs to (the toolkit has no /v1/config consumer).", "fix_direction": "Make MergeOpenCode merge per-entry instead of wholesale-replacing while the backend is loading, or refuse to write until the backend answers.", "test_plan": "RED: backend loading + GET /v1/config/opencode must not clobber user entries; GREEN guard on merged output; paired mutation restoring wholesale replace must FAIL."},
  {"id": "SELF-13", "repo": "helix_llm", "severity": "MEDIUM", "file": "commit 52c827f", "defect": "A commit titled as a 32-line FAQ addition carries 1424 lines of another agent's in-flight gateway work — git add <path> stages the path, but git commit commits the whole index. Deliberately not rewritten; record is complete (834971c).", "fix_direction": "None — corrected discipline recorded (pathspec-limited commit or git diff --cached --stat immediately before committing).", "test_plan": "n/a (process)."},
  {"id": "HXA-testharness-1", "repo": "helix_agent", "severity": "MEDIUM", "file": "tests/integration (5 tests) + bin/helixagent (gitignored)", "defect": "Five integration tests require the gitignored build artifact bin/helixagent and failed because nobody built it — a harness gap, not an environment gap. Binary now exists; the build-and-rerun verification is blocked by another agent's in-flight compile error at internal/handlers/openai_compatible.go:1367.", "fix_direction": "Have the harness build the artifact (or SKIP naming it); re-run the five tests once the tree compiles.", "test_plan": "Run the five named integration tests with the binary built; capture per-test pass/fail as the owed verification."},
  {"id": "HXA-testharness-2", "repo": "helix_agent", "severity": "MEDIUM", "file": "tests/integration/cli_schema_validation_test.go:166", "defect": "TestOpenCodeSchemaValidation reads the operator's real ~/.config/opencode/opencode.json and Fatalf's when absent — a PASS asserts a fact about the operator's machine, not the code; can also fail on any machine without OpenCode.", "fix_direction": "Decide intent: SKIP-with-reason when the optional third-party tool is absent, or generate a fixture config into a temp dir and validate that.", "test_plan": "RED on a host without the file: test must SKIP naming the missing prerequisite, never Fatalf; sandboxed-fixture variant asserting schema validity of a generated config."},
  {"id": "NOTE-5", "repo": "helix_llm", "severity": "INFO", "file": "n/a — label-method note (progress.yml:2513)", "defect": "'environment-bound' label checked three times, wrong all three — proving a failure is not yours ends the inquiry too early.", "fix_direction": "None — method note.", "test_plan": "n/a."},
  {"id": "OPEN-30", "repo": "helix_llm", "severity": "LOW", "file": "internal/naming RetiredHosts", "defect": "The retired-host set is exactly {localhost, 127.0.0.1} deliberately; 0.0.0.0 and ::1 are absent until evidence they were ever published — widening is the one direction that cannot be walked back cheaply.", "fix_direction": "Revisit only if a deployment is found that published those forms.", "test_plan": "n/a (deliberate, evidence-gated)."},
  {"id": "NOTE-6", "repo": "helix_llm", "severity": "INFO", "file": "n/a — T055 rounds note (progress.yml:2912)", "defect": "T055 round 1 found 7, round 2 found 4 (three created by round-1 fixes), round 3 found 2 — the iterate-to-a-clean-GO argument.", "fix_direction": "None — method note.", "test_plan": "n/a."},
  {"id": "NOTE-7", "repo": "helix_llm", "severity": "INFO", "file": "loopback_serving_host_test.go", "defect": "A skip whose condition is the defect's own precondition (machine named localhost) is a blind spot aimed at the target — a green suite never showed the gap.", "fix_direction": "The defect it guarded is fixed (F-1-resolved); carry the skip-correlation check for future skips.", "test_plan": "n/a — lesson carried."},
  {"id": "SELF-16", "repo": "helix_llm", "severity": "MEDIUM", "file": "internal/brain/brain.go (mutation cycle)", "defect": "A restored file (diff byte-identical) followed by a FAIL that was the pre-restore cached outcome — diff proves the file changed, not that the test binary did.", "fix_direction": "None — practical rule recorded: go clean -testcache after any restore before believing a PASS or FAIL.", "test_plan": "n/a (method)."},
  {"id": "OPEN-32", "repo": "helix_llm", "severity": "LOW", "file": "n/a — hawkscan post-commit hook (environmental)", "defect": "The hook announced a helix_llm commit SHA (1f97cf1) in a helix_agent session, including once when nothing was committed — cross-repo evidence mis-attribution.", "fix_direction": "Environmental; recorded so nobody relies on its attribution without checking.", "test_plan": "n/a."},
  {"id": "OPEN-33", "repo": "helix_llm", "severity": "MEDIUM", "file": "data/vision_image.yaml", "defect": "Five requires_accelerator:false entries whose figures were never re-examined for RAM-vs-VRAM sourcing; the selection fix bounds the offer through laneboot but whether the recorded numbers mean what text.yaml's mean is unverified.", "fix_direction": "Same class as OPEN-5, own pass: verify the five figures' memory kind and correct or annotate provenance.", "test_plan": "Per-entry provenance test asserting the recorded figure names its memory kind; mutation mislabelling the kind must FAIL."},
  {"id": "OPEN-34", "repo": "helix_llm", "severity": "LOW", "file": "cmd/helixllm (TestChatCompletions_HostWithNoListedModelsIsStillLive/host_127.0.0.1)", "defect": "Intermittent failure under parallel full-suite runs; isolated properly (reproduces with the reporting agent's change removed, passes 3/3 alone, absent under -p 1) but the mechanism is UNCONFIRMED — possibly a 2s /health probe under load, which would mean a path still probes despite the F-1 registry-only design.", "fix_direction": "Measure before labelling: determine whether the verdict still depends on a live /health observation; if yes, that is a real path, not a flake.", "test_plan": "Load-instrumented repetition distinguishing legitimate timing from a race; if a probe path exists, registry-only assertion test with paired mutation."},
  {"id": "METHOD-1", "repo": "helix_llm", "severity": "LOW", "file": "n/a — method note", "defect": "Running four suites concurrently manufactured false failures in three (timing, ephemeral-port exhaustion, live-server interference) — §11.4.119 single-resource-owner applied to CPU, ports and localhost.", "fix_direction": "Timing/port suites run with exclusive host ownership; verdicts under self-inflicted contention are not evidence.", "test_plan": "n/a (method)."},
  {"id": "OBS-1", "repo": "helix_code", "severity": "LOW", "file": "internal/server/server.go:235-240,247 (+ handlers_test.go:743,808)", "defect": "GET /api/v1/auth/me answers 404 because the route was never registered in the live router — only in a test-only router the scrape picked up. The real endpoint is /api/v1/users/me (users.GET at :247).", "fix_direction": "Decide which path to publish (mount auth/me as an alias or fix the consumers/docs naming users/me).", "test_plan": "Route-registration test asserting the chosen path answers 200 with a valid bearer and 401 without; negative test asserting no phantom test-only routes leak into docs."},
  {"id": "TOOLKIT-1", "repo": "claude_toolkit", "severity": "LOW", "file": "scripts/tests/test_claude.sh:7", "defect": "Test hardcodes ALIAS_FILE=$HOME/.local/share/claude-multi-account/aliases.sh and never sources lib/sandbox.sh — asserts on the operator's live machine state and can pass on a stale alias for a provider that no longer verifies.", "fix_direction": "Drive assertions from a sandbox alias file generated from a known provider set, per lib/sandbox.sh.", "test_plan": "Sandboxed run: generate alias file from a fixed provider set; all assertions read it; mutation flipping one provider's verification state must flip the outcome."},
  {"id": "TOOLKIT-2", "repo": "claude_toolkit", "severity": "MEDIUM", "file": "scripts/tests/test_providers.sh", "defect": "The semantic judge cannot run in this environment and returns skip/exit 2, so 11 downstream providers-semantic assertions fail behind one root cause — on the unmerged fix/helixllm-export-review-findings branch.", "fix_direction": "Diagnose on a host where the judge can run (one root cause, not eleven defects); land with the branch-merge decision.", "test_plan": "Run the providers-semantic block on a judge-capable host; assert real verdicts (verified/unverified) not skip; capture pass/fail per assertion."},
  {"id": "TOOLKIT-3", "repo": "claude_toolkit", "severity": "LOW", "file": "scripts/claude-providers.sh (_cma_helixllm_fetch_models)", "defect": "A reachable-but-untrusted host (self-signed cert, curl exit 60) is reported as 'did not answer with a model listing' — reads as 'host is down' when it is answering fine; also appends /models to the base so the base must already contain /v1.", "fix_direction": "Distinguish unreachable from untrusted in the message (surface curl exit 60 distinctly); document the /v1 base requirement.", "test_plan": "Fixture test against a self-signed endpoint asserting the message names the trust failure, not unreachability; mutation collapsing the two messages must FAIL."},
  {"id": "BOOT-4", "repo": "helix_code", "severity": "MEDIUM", "file": "Dockerfile build context vs helix_code/go.mod (43 replace directives)", "defect": "The container build cannot resolve the 43 replace directives pointing at ../submodules/* outside the copied directory; all 43 targets exist locally totalling 4.2 GB. Workaround in use: the native path (make build), which Rule 4 names first.", "fix_direction": "Mirror the repo layout in the image plus a carefully scoped .dockerignore so the build context is not multi-GB — its own piece of work.", "test_plan": "Container build test asserting go build succeeds inside the image; context-size assertion bounding the build context."},
  {"id": "TEST-2", "repo": "helix_code", "severity": "MEDIUM", "file": "tests/memory getTestBaseURL()", "defect": "The whole suite silently skips when no server is up and reports 'ok ... 0.077s' having exercised nothing (§11.4.238 coverage gap) — the reason the leak signal went unseen for a day. The leak signal itself was refuted on a quiet host (15.5% rise, tail falling).", "fix_direction": "Make absence of a server a loud, counted SKIP (or boot one); keep the two-part leak guard (R2 + magnitude).", "test_plan": "Run with no server: package must report SKIP-with-reason, never bare ok; run with a server: the leak test executes and its verdict is honoured."},
  {"id": "BOOT-8", "repo": "helix_llm", "severity": "MEDIUM", "file": "internal/shared/config/config.go:47", "defect": "Hardcoded default HELIX_LLM_LOCAL_MODEL = Llama-3.1-70B-Instruct-Q4_K_M (~40 GB) on a 30 GB RAM / 12 GB VRAM host — offer-what-cannot-start sitting in a default value; bites any fresh checkout without .env. Host itself now serves a 3B via .env.", "fix_direction": "Change the default to a footprint-empty state (require explicit selection, letting feature-002 capability selection choose) rather than another hardcoded model.", "test_plan": "Config-default test asserting no unservable-by-any-measured-host default ships; boot test asserting a fresh checkout without .env offers nothing it cannot start."},
  {"id": "BOOT-9", "repo": "helix_code", "severity": "MEDIUM", "file": "config/config.yaml local: (localhost:11434) + /api/v1/llm/providers", "defect": "ollama is disabled in config yet local: still points at dead 127.0.0.1:11434 with enabled:true, and /api/v1/llm/providers lists ollama status:available from config without a probe — the port refuses.", "fix_direction": "Probe-derived status (or remove the stale local: entry); a provider listed available must answer a probe.", "test_plan": "With :11434 refusing, assert ollama is reported unavailable/absent; mutation reintroducing the config-derived status must FAIL."},
  {"id": "ROOT-1", "repo": "claude_toolkit", "severity": "HIGH", "file": "scripts/providers/helixagent.json (18434/v1), helixagent-native.json (18435), helixllm-gateway.json (18435/v1), claude-providers.sh:400,428,2084, lib.sh:2189", "defect": "Hardcoded provider base_urls point at ports nothing serves; :18434 now answers 200 as llama-server (qwen2.5-coder-3b) — a LIVE BUT WRONG service that will connect and mis-serve rather than fail loudly. :18435 dead on both paths. Real listeners: helixagent :7061/:8111, helixllm :8443 (HTTPS).", "fix_direction": "Update the three provider JSONs + script fallbacks to the real endpoints (:7061/v1, :8443 with /v1 base and CA bundle), or resolve endpoints from a single source of truth; then T052 sync can verify.", "test_plan": "After the change: claude-providers verification completes a real request per provider (verified state, *_verified.json produced); negative test asserting no base_url targets a port owned by a different service."},
  {"id": "SECURITY-5", "repo": "helix_llm", "severity": "HIGH", "file": "docs/manual/security.md + docs/manual/modules.md", "defect": "Security manual documented JWT authentication that does not exist (operator could set HELIX_AUTH_JWT_SECRET, tick the checklist, and believe the server protected while API-keys sat empty). Docs layer fixed — section says NOT IMPLEMENTED, checklist item struck through.", "fix_direction": "Operator decision: implement JWT auth (digital.vasic.auth replace lacks a require; golang-jwt not in go.mod) or drop the field — now an informed decision per the §11.4.124 investigation.", "test_plan": "If implemented: end-to-end JWT issue/verify/expire tests + blank-credential guard already landed. If dropped: config-refusal test naming the field."},
  {"id": "SECURITY-6", "repo": "helix_llm", "severity": "HIGH", "file": "docker-compose.mcp-full.yml", "defect": "The Go catalogues are fixed but docker-compose.mcp-full.yml still launches three npm package names held for security.", "fix_direction": "Remove or replace the three held npm names in the compose file with the fixed Go equivalents.", "test_plan": "Compose config test asserting no security-held package name appears; grep-guard wired into the suite."},
  {"id": "HXC-prodconfig", "repo": "helix_code", "severity": "HIGH", "file": "config/production-config.yaml", "defect": "22 keys — whole environments, features, performance and security sections — are configured at length and read by nothing; registered as inert with per-key reasons. logging.file inert while logging.output is read, so the two can disagree silently.", "fix_direction": "Operator decision per key: wire, remove, or keep-declared-inert; the startup banner already says they are inert.", "test_plan": "Per-key inertness test (config-key vs Config-field diff) kept green; whichever keys get wired gain honoured-value assertions; paired mutation marking an inert key as read must FAIL."},
  {"id": "MILESTONE-green", "repo": "helix_llm", "severity": "INFO", "file": "n/a — suite state note (progress.yml:2130)", "defect": "helix_llm fully green: 53 packages, 0 failures on a quiet tree — point-in-time, not a completion claim (8 tasks + ~20 findings open at the time).", "fix_direction": "None — state note.", "test_plan": "n/a."},
  {"id": "T055-1", "repo": "helix_llm", "severity": "CRITICAL", "file": "cmd/helixllm/main.go:461; internal/brain/brain.go:191,273", "defect": "RECONSTRUCTED (report not on disk): production HTTP path never resolved the published model identifier — fallback.Chain overwrites req.Model with each provider's FIRST model; a client asking helixllm-gpu-01-qwen2-5-7b-... was answered by chutes/deepseek-chat.", "fix_direction": "Already fixed + guarded (Brain.PinModel resolves identifier or raw served name to the serving provider, honouring the identity's HOST); verify the guard rather than re-derive.", "test_plan": "Existing HTTP-layer tests with fake providers recording what they were asked (CRITICAL-7-resolved evidence); mutation breaking PinModel must FAIL. UNCONFIRMED: exact round-1 severity mapping."},
  {"id": "T055-2", "repo": "helix_llm", "severity": "HIGH", "file": "internal/naming derive/ServingHost identity rendering (exact line UNCONFIRMED)", "defect": "RECONSTRUCTED: the published identity named 127.0.0.1 — it named no machine and collided across machines (two-hosts-collide class).", "fix_direction": "Already fixed (identity renderings retired via RetiredHosts + registry-aware resolution; see f63b96f, F-1-resolved).", "test_plan": "Existing registry/retired-identifier tests incl. the five-host 404/503 table; mutation re-admitting a loopback identity must FAIL. UNCONFIRMED: item-to-number mapping."},
  {"id": "T055-3", "repo": "helix_llm", "severity": "HIGH", "file": "internal/naming/export.go + HelixCode/OpenCode exporters (call sites: zero)", "defect": "RECONSTRUCTED: export dead-code — the HelixCode and OpenCode exporters had no caller at all; declared, documented, unreachable (W2a-3 names this class 'export dead-code').", "fix_direction": "Already resolved (OPEN-17/18-resolved) — exporters wired or removed per that entry.", "test_plan": "Call-site existence test (exporter entry points reachable from a production binary); mutation deleting the wiring must FAIL. UNCONFIRMED: item-to-number mapping."},
  {"id": "T055-4", "repo": "claude_toolkit", "severity": "HIGH", "file": "scripts/claude-providers.sh helixllm-export (exact line UNCONFIRMED)", "defect": "RECONSTRUCTED ('corrupted identities'): jq @tsv escaping claimed to break the common case of org/model and hf.co names in exported identities.", "fix_direction": "Severity claim REFUTED — @tsv escapes only backslash, tab, newline, CR (verified byte-identical pass-through of 'helixllm/h/org/llama3:8b'); a narrower real fix shipped + guarded (TOOLKIT-1-2-3 FINDING 1).", "test_plan": "Existing identity-serialisation tests incl. backslash/control-char names; mutation restoring unescaped output must FAIL."},
  {"id": "T055-5", "repo": "claude_toolkit", "severity": "HIGH", "file": "export path — discovery Bearer secret (exact line UNCONFIRMED)", "defect": "RECONSTRUCTED ('misdirected credential'): the discovery secret was disclosed to every configured host; the predicted 401 impact was REFUTED — the server default is open access, so requests succeeded while leaking the secret.", "fix_direction": "Already fixed + guarded (TOOLKIT-1-2-3 FINDING 2): gateway key arrives while the secret reaches no server and no file; scoped window assertions total-vs-keyed.", "test_plan": "Existing credential-flow tests asserting the secret reaches no server and no file; mutation re-disclosing must FAIL."},
  {"id": "T055-6", "repo": "claude_toolkit", "severity": "HIGH", "file": "scripts/claude-providers.sh --apply retirement gate (test lock-in at :474)", "defect": "RECONSTRUCTED ('add-only --apply'): a withdrawn model's .env and alias survived retirement and stayed invocable; round 2 then found the first fix FAILED OPEN (empty listing on a restarting host retired everything and cmd_remove rm -f'd the config).", "fix_direction": "Already fixed + guarded (TOOLKIT-1-2-3 FINDING 3 + F2-resolved): three-gate deletion safety — toolkit-written marker, host ANSWERED this run, host no longer lists it; silence is never evidence; removal moves the directory aside.", "test_plan": "Existing retirement-gate tests incl. the empty-list-during-loading case and the mutation that re-opens the data-loss gate; all must hold."},
  {"id": "T055-7", "repo": "helix_llm", "severity": "CRITICAL", "file": "naming/export design — OpenAI-compat local backend admission (exact line UNCONFIRMED)", "defect": "RECONSTRUCTED: review claimed OpenAI-compat local backends are 'silently dropped' from naming/export.", "fix_direction": "REFUTED as deliberate design — fixing it would stamp identities on proxied vendor models, which the design forbids; no change (NOTE-4).", "test_plan": "Design-decision test asserting proxied vendor models never receive identities; no code change owed."}
]
```

### Anomalies (crossed the anti-bluff bar — flagged, not silently reconciled)

1. **EX-5 is stale**: ledger status `recorded`, but the tree shows the defect already fixed
   (commit `ab34fa8 fix(gateway): a listing of nothing is an empty list, not null`;
   the fabricated list survives only as an explanatory comment at
   `internal/gateway/openai.go:524`). Ledger row needs a status update, not a fix.
2. **T055 report not on disk**: the verbatim 7-finding export-review report exists only as
   cross-references inside progress.yml. The T055-1..7 enumeration above is a
   reconstruction; items T055-2, 3, 7 are UNCONFIRMED as to exact wording/severity, and
   the design doc's "7 critical/high" vs NOTE-6's "Round 1 found 7" could disagree by one
   item depending on whether the LOW withheld-vocabulary finding is counted.
3. **T055 task checkbox out of sync**: tasks.md still shows T055 unchecked even though
   progress.yml records three completed review rounds (7 + 5 + 2 findings, all closed).
4. **W2a-3 premise inverted**: the plan's W2a-3 assumes T055-1..7 are unfixed and tells
   Wave 2 to write failing tests and fix minimally. The ledger shows all seven are
   already fixed+guarded or refuted — W2a-3 should become a guard-verification pass
   (controller decision).
5. **TOOLKIT-1-2-3** is `fixed + guarded (commit pending final suite)` — excluded from
   "open" by the status rule but its commit landing is still owed.
6. **CRITICAL-1-RESOLVED** is FIXED but explicitly leaves 83 real challenge failures
   "awaiting their own work" — covered by the open CHALLENGES-triage re-measure.

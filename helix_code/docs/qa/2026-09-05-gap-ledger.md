# Gap Ledger — helix_code consumption (Spec 002: local LLM execution via llama.cpp + Colibri, dynamic hardware-based model selection)

- **Date:** 2026-09-05
- **Scope:** `helix_code/` Go module (`dev.helix.code`) — `internal/llm/**`, `internal/config/**`, `internal/server/**`, `config/config.yaml`, `cmd/**`
- **Method:** read-only forensic extraction (Steps 1–4 of task-F3 brief). Every entry carries a file:line actually read during this pass. Unknowns marked `UNCONFIRMED:`.
- **Anti-bluff:** no entry asserts runtime behaviour that was not read in source. Live reachability of the HelixLLM gateway (:8443) and coder (:18434) was NOT probed in this pass (read-only mandate) — marked UNCONFIRMED where relevant.

## Step 1 — Default provider resolution (trace)

| Link | Evidence |
|---|---|
| Config default | `internal/config/config.go:676` — `v.SetDefault("llm.default_provider", "local")` |
| Struct field | `internal/config/config.go:199` — `LLMConfig.DefaultProvider` (`mapstructure:"default_provider"`) |
| Server resolver | `internal/server/llm_generate.go:175-267` — `resolveLLMProvider(providerName, model)` |
| Config NOT consulted | `internal/server/llm_generate.go:176-180` — `SelectorInput{Flag, Env, Config: ""}`; `Config` is always empty |
| `"local"`/`"helixllm"` name → coder | `internal/server/llm_generate.go:202-204` → `resolveHelixLLMLocalProvider` (`:553-568`), base URL `http://localhost:18434` (`:530`) via `HELIX_LLM_LOCAL_OPENAI_ENDPOINT` (`:524`) |
| No provider named → Ollama | `internal/server/llm_generate.go:251-266` — `llm.NewOllamaProvider`, `http://localhost:11434` (`:594`) |
| `Select` honours Config when set | `internal/llm/provider_factory.go:57` — `firstNonEmpty(input.Flag, input.Env, input.Config)` |
| Handler entry | `internal/server/llm_generate.go:330` — `llmProviderResolver(req.Provider, req.Model)`; wire_facade `:638`,`:691` pass `""` |
| CLI `helix generate` | `cmd/other_commands.go:116-134` — reads `cfg.LLM.DefaultProvider`, casts to `llm.ProviderType`, feeds `ModelManager` |

**Finding:** config `llm.default_provider: "local"` is never threaded into `SelectorInput.Config` on the server path. A provider-less request therefore falls through to Ollama `:11434`, not the config-declared `"local"` route (`:18434`). On the CLI path, `NewModelManager()` registers zero providers (`internal/llm/model_manager.go:70-76`), so `SelectOptimalModel` always fails with "no models available" (`:109-110`) regardless of config.

## Step 2 — Dead config block (verbatim capture, `config/config.yaml`)

Lines 154–173 (verified by Read at 2026-09-05; block begins at the `# HelixAgent providers` comment on line 154):

```yaml
    # HelixAgent providers
    helix-llm:
      type: helix-llm
      endpoint: "${HELIX_LLM_ENDPOINT:http://localhost:8081}"
      enabled: true
      parameters:
        timeout: 60.0
        max_retries: 3
        streaming_support: true
        api_key: "" # Set via HELIX_LLM_API_KEY
      
    helix-debate:
      type: helix-debate
      endpoint: "${HELIX_DEBATE_ENDPOINT:http://localhost:8082}"
      enabled: true
      parameters:
        timeout: 120.0
        max_retries: 3
        streaming_support: true
        api_key: "" # Set via HELIX_DEBATE_API_KEY
```

Deadness evidence: provider-type switch in `internal/llm/provider_factory.go:83-113` has no `helix-llm`/`helix-debate` case; zero non-comment Go references to either string (grep 2026-09-05); defaults point at the wrong processes (`:8081` = HelixCode server itself, `:8082` = ChromaDB — documented at `config/replica-8081.yaml:187-196`). Worse, the ENTIRE `llm.providers` block is discarded at load: `LLMConfig` declares no `Providers` field (`internal/config/config.go:197-226`), and `internal/config/strict.go:315-317` reports the whole block as discarded. `HELIX_LLM_ENDPOINT` / `HELIX_LLM_API_KEY` are read by no Go code (only `.env.example:44,50` and comments); `HELIX_DEBATE_ENDPOINT` has zero occurrences outside this YAML block.

## Step 3 — Gateway wiring (HelixLLM gateway https://127.0.0.1:8443)

- Zero code references to `:8443` for LLM routing (grep over `*.go/yaml/yml/json/toml/sh/service/md` in the module). Only mentions: a comment at `config/replica-8081.yaml:189` ("The real HelixLLM gateway is https://localhost:8443 (TLS) per setup.sh:159") and unrelated test port lists (`tests/e2e/test_bank/performance/performance_security_tests.go:1313`, `internal/discovery/*_test.go`).
- The only "helixllm" route in the server is hardwired to the llama.cpp coder sidecar at `http://localhost:18434` (`internal/server/llm_generate.go:530,553-568`) — plain HTTP, loopback, no TLS, no gateway endpoint config key, no API-key handling.
- No config key or env var names the gateway (`HELIXLLM_GATEWAY`, `HELIX_LLM_GATEWAY`, etc.: zero matches).

**Gap:** spec 002's "all local LLM execution via llama.cpp + Colibri" has no consumption path of the HelixLLM gateway in this module; dynamic hardware-based model selection has no wiring surface here either (see entry 05).

## Ledger

| id | repo | severity | file | defect | fix_direction | test_plan |
|---|---|---|---|---|---|---|
| HXC-002-F3-01 | helix_code | critical | `internal/server/llm_generate.go:176-180` (+`:330`, `internal/config/config.go:676`) | Config `llm.default_provider: "local"` is never consumed by the server generate path — `SelectorInput.Config` is hardcoded `""`, so provider-less requests fall to Ollama `:11434` (`:251-266`) while the config-declared `"local"` route (`:18434`, `:202-204`) is only reachable by explicitly naming `local`/`helixllm`. Config intent and runtime behaviour diverge silently. | Thread `cfg.LLM.DefaultProvider` into `SelectorInput.Config` (precedence Flag > Env > Config already honoured at `internal/llm/provider_factory.go:57`); ensure `"local"` in config resolves to the helixllm coder route, not Ollama. | Unit (RED→GREEN): `resolveLLMProvider("","")` with config default `local` must construct a provider whose BaseURL is the coder endpoint (`:18434`), not Ollama — polarity-switch `RED_MODE` per §11.4.115; live round-trip test mirroring `llm_generate_helixllm_live_test.go:53` against the coder `/v1/models`. |
| HXC-002-F3-02 | helix_code | critical | `config/config.yaml:154-173` | Dead provider block `helix-llm` / `helix-debate`: unknown types (no factory case at `internal/llm/provider_factory.go:83-113`), zero Go references, wrong default endpoints (`:8081` self, `:8082` ChromaDB per `config/replica-8081.yaml:187-196`). Entire `llm.providers` block is discarded at load — `LLMConfig` has no `Providers` field (`internal/config/config.go:197-226`), `internal/config/strict.go:315-317` reports it discarded. | Remove the verbatim block (captured in Step 2 above) from `config/config.yaml`; drop dead keys `HELIX_LLM_ENDPOINT`/`HELIX_LLM_API_KEY` from `.env.example:44,50`; if a gateway-backed provider is wanted, add it as a real factory type (see F3-03) rather than resurrecting this block. | RED: startup strict-load warning (`strict.go`) must no longer list `llm.providers` as discarded-after-parse for these two entries; grep gate: zero references to `helix-llm`/`helix-debate` outside the removal commit; config load test asserting no `ErrorUnused` for the removed block. |
| HXC-002-F3-03 | helix_code | high | `internal/server/llm_generate.go:530,553-568` | No route to the HelixLLM gateway (`https://127.0.0.1:8443`, user unit `helixllm-gateway.service`): zero code/config references to `:8443` (only comment `config/replica-8081.yaml:189`). The `helixllm`/`local` selector is hardwired to the llama.cpp coder at `http://localhost:18434`, plain HTTP, no TLS config, no gateway env key, no API-key handling. UNCONFIRMED: gateway live reachability not probed (read-only mandate). | Add a gateway endpoint key (env `HELIX_LLM_GATEWAY_ENDPOINT` or config-injected per §11.4.28) and a provider route constructing an HTTPS client against the gateway with TLS trust config; keep the `:18434` coder route as the direct-sidecar fallback. | Unit: resolver mapping `gateway`/`helixllm-gateway` selector → BaseURL `https://127.0.0.1:8443`; live test: `GET /v1/models` on the gateway (TLS) answered → completion round-trip, mirroring `llm_generate_helixllm_live_test.go` pattern with SKIP-when-unreachable per §11.4.3 (no fake PASS). |
| HXC-002-F3-04 | helix_code | high | `cmd/other_commands.go:116-134` (+`internal/llm/model_manager.go:70-76,109-110`) | `helix generate` CLI is non-functional: `NewModelManager()` registers zero providers, so `SelectOptimalModel` always returns "no models available" regardless of `llm.default_provider`; `ProviderType("local")` is not a registered/known type in the empty map. `cfg.LLM.DefaultProvider` is read but cannot resolve to any provider instance. | Route `helix generate` through the same resolver as the server (`resolveLLMProvider` semantics) or register providers from config into `ModelManager` before selection; make `local` map to the coder/gateway route. | RED: `helix generate "prompt"` with default config currently errors "no models available" (capture); GREEN: same command returns a real completion from the local route; regression test pinning the registration path so the empty-map defect cannot recur (§11.4.135 guard). |
| HXC-002-F3-05 | helix_code | medium | module-wide (zero matches) | "Colibri" is entirely absent from the Go module — zero case-insensitive matches for `colibri` in any `.go/.yaml/.md/.sh`. Spec 002's "dynamic hardware-based model selection via Colibri" has no implementation surface in `helix_code`; closest existing machinery is `ModelManager`'s hardware detector hook (`internal/llm/model_manager.go:70-72`, `hardware.NewDetector()`) with no Colibri policy on top. UNCONFIRMED: submodules outside `helix_code/` (e.g. `submodules/helix_llm`) not scanned in this pass. | Locate/confirm the Colibri component (submodule catalogue check per §11.4.74); define the model-selection contract (hardware profile → model tier) as config-injected data; wire it between hardware detection and provider/model selection. | Unit: hardware-profile → model-selection table with boundary cases; integration: selection against the real coder catalog (`/v1/models`) asserting a verifier-sourced model per CONST-036; honest SKIP-with-reason if Colibri service absent. |
| HXC-002-F3-06 | helix_code | medium | `.env.example:50` vs `internal/server/llm_generate.go:619-630` | Env-var contract drift for local endpoints: `.env.example` documents `HELIX_LLAMA_CPP_HOST=http://localhost:8080`, but :8080 is HelixCode's OWN server port — requests would POST completions to itself (measured 404/502 documented at `llm_generate.go:619-630`); code default is `:18434`. Dead keys `HELIX_LLM_ENDPOINT`/`HELIX_DEBATE_API_KEY`/`HELIX_DEBATE_ENDPOINT` read by no Go code. **CORRECTION (2026-09-05, §11.4.6):** the `:8080` claim above was already stale when this ledger was authored — the `:8080`→`:18434` fix landed in commit `a74ae7cb`, an ancestor of this ledger's own commit `502b45d9`. The residual real work — the two genuinely-dead keys `HELIX_LLM_API_KEY`/`HELIX_LLM_ENDPOINT` (the `HELIX_DEBATE_*` keys were already absent from `.env.example`) — has now been removed. | Align `.env.example` with the real defaults (`:18434` coder, `:11434` ollama), remove dead gateway/debate keys, and point operators at the single override convention (`HELIX_LLM_LOCAL_OPENAI_ENDPOINT`). | RED: load `.env.example` defaults → resolver must not construct a provider pointing at `:8080`; unit asserting `envLlamaCppHost()` precedence (explicit env > `HELIX_LLM_LOCAL_OPENAI_ENDPOINT` > `:18434`) per `llm_generate_llamacpp_local_test.go:327`. |
| HXC-002-F3-07 | helix_code | medium | `internal/providers/ai_integration.go:286-300` (+`internal/llm/litellm/unified_provider.go:20`, `internal/llm/providers/cerebras/cerebras.go:77`) | The process-global cloud gate is enforced at llm.NewCloudProvider and (since 2026-09-06) at llm.NewProvider, but three constructor families bypass BOTH by calling the concrete hosted constructors directly: createAIProvider (internal/providers/ai_integration.go:286-300) dispatches to NewOpenAIProvider/NewAnthropicProvider/NewGeminiProvider with no gate check; litellm.NewUnifiedProvider (internal/llm/litellm/unified_provider.go:20) builds a bearer-auth HTTP client against an arbitrary endpoint; internal/llm/providers/cerebras/cerebras.go:77 likewise. VERIFIED LATENT, NOT LIVE (2026-09-06): createAIProvider is unexported, and package internal/providers is imported by exactly one non-test file (internal/i18nwiring/wire.go:169) which registers i18n translators and constructs no provider; litellm.NewUnifiedProvider has one in-package caller (litellm/registry.go:29) and is referenced only by internal/clarification, whose Engine nothing instantiates; cerebras has zero non-test constructor callers. No shipping path reaches an ungated cloud constructor today — but the gate is bypassed silently the moment internal/providers is wired in. | Prefer routing createAIProvider through the gated llm.NewProvider (small, testable, keeps the gate in one place). Gating inside each concrete hosted constructor gives higher assurance but has a large blast radius: dozens of existing tests construct NewAnthropicProvider/NewBedrockProvider directly and would all need §11.4.120 reconciliation in one change. | RED: with the gate CLOSED, a guard driving createAIProvider (or its exported entry, once one exists) for a hosted ProviderType must refuse with an error wrapping ErrCloudDisabled — today it constructs. Plus a package-boundary gate asserting no non-test file outside internal/llm calls llm.New{OpenAI,Anthropic,Gemini,Bedrock,Cohere,Mistral}Provider directly; paired §1.1 mutation adds such a call and asserts the gate FAILs. **PARTIALLY RESOLVED 2026-09-06 — the `internal/providers` half is CLOSED; the two `internal/llm` files are ASSESSED ONLY (owned by another stream, not edited).** LATENCY RE-VERIFIED INDEPENDENTLY by execution (not inherited): `internal/providers` has exactly ONE non-test importer, `internal/i18nwiring/wire.go:169`, which uses exactly one symbol from it (`providerspkg.SetTranslator` at `:530`) and constructs no provider; and `NewAIIntegration` — the sole route to `createAIProvider`, via `(*AIIntegration).Initialize:250` — has ZERO non-test callers. FIX CHOSEN — gate AT the dispatch, not routing through `llm.NewProvider`: routing would need an adapter between the two distinct interfaces (`providers.AIProvider` vs `llm.Provider`), whereas the in-place gate adds ZERO new imports because `ai_integration.go` already imports `internal/llm` (so no cycle risk) — the genuinely smaller and more testable change. GATED SET DETERMINED BY READING EVERY ARM'S BODY, not by which type names sound hosted: exactly three arms delegate to a concrete hosted constructor (`openai`→`llm.NewOpenAIProvider`, `anthropic`→`llm.NewAnthropicProvider`, `gemini`→`llm.NewGeminiProvider`); the other ten (cohere, huggingface, mistral, gemma, llamaindex, memgpt, crewai, characterai, replika, anima) return `newNotImplementedProvider` — they construct nothing and dial nothing, so gating them would break local routes for no safety gain. EXECUTED PROOF: RED pre-fix — all three hosted subtests got a nil error with the gate CLOSED, and the run log emitted `✅ OpenAI provider initialized with 4 models`, i.e. the bypass OBSERVED at runtime with credentials present, not inferred from source; GREEN post-fix; three paired §1.1 mutations each killed exactly its intended test and nothing else — delete the gate → the three hosted subtests FAIL; invert the predicate (gate everything) → the non-hosted positive control FAILs; ignore the gate state (always refuse hosted) → the gate-OPEN test FAILs — with every restore sha256-identical and a clean residue scan. `make verify-compile` (incl. test-code compile), `vet`, and the FULL `internal/config` + `internal/providers` packages all pass. Guard: `internal/providers/cloud_gate_dispatch_test.go`. AST-SCAN FEASIBILITY (measured answer to this entry's own proposal): a name-based ban on `llm.New{OpenAI,Anthropic,Gemini,Bedrock,Cohere,Mistral,...}Provider` outside `internal/llm` is NOT landable as specified. The measured population of such non-test call sites is exactly FOUR in TWO files: the three now-GATED sites in `ai_integration.go:1783,1813,1858` (which a name ban would flag even though they are correct) and `tests/integration/cmd/p1f15_challenge/main.go:337`, a non-test challenge binary outside this stream's scope. The correct form is gate-PROXIMITY AST analysis — "is this call dominated, within its own function, by a `CloudEnabled()` / `ErrCloudDisabled` guard?" — which IS feasible (`internal/llm/cloud_gate_redaction_scan_test.go`'s WalkDir+AST walk is the working template) but must land together with a decision on the p1f15 challenge site, so it is a SEPARATE work item, not a rider on this one. REMAINING OPEN (assessment for the owning stream, reachability verified read-only): (1) `internal/llm/litellm/unified_provider.go:20` — LATENT: `NewUnifiedProvider`'s only non-test caller is `(*Registry).Register` (`litellm/registry.go:29`), and `Registry.Register` / `litellm.NewRegistry` have ZERO non-test callers anywhere; the only non-test importers of the package are `internal/clarification/{engine,question}.go`, which take a `*litellm.UnifiedProvider` as a constructor PARAMETER and never build one, and `internal/clarification` itself has one non-test importer (`i18nwiring/wire.go:103`) using only `SetTranslator` (`:299`), while `clarification.NewEngine`'s only non-test caller is its sibling `NewQuestionGenerator` (`question.go:18`), itself uncalled. FIX SHAPE: unlike the named-vendor arms this one targets an ARBITRARY endpoint, so the predicate must be ENDPOINT-locality, not a type name — `internal/llm` already has exactly that in `isLocalEndpointURL` (provider_factory.go, the documented companion to `isLocalProviderType`); it is unexported today, so the owning stream either exports it or puts the check inside package `llm` and has litellm call through it. (2) `internal/llm/providers/cerebras/cerebras.go:77` — LATENT and the most isolated of the three: `cerebras.NewProvider` has ZERO non-test importers of its package (the only non-test textual match outside the file is a self-referential comment at `cerebras.go:59`) and ZERO non-test constructor calls. FIX SHAPE: Cerebras is an unambiguously hosted vendor, so it takes the simple form — a `!llm.CloudEnabled()` check at the top of `NewProvider` returning an error wrapping `llm.ErrCloudDisabled`; the package already imports `llm` (its signature takes `llm.ProviderConfigEntry`), so zero new imports and no cycle risk, the same property that made the `internal/providers` fix small. |
| HXC-002-F3-08 | helix_code | low | `internal/config/config.go.clean:153,155` | internal/config/config.go.clean is a TRACKED but never-compiled shadow of config.go (the extension is not .go, so the toolchain never sees it). It still carries at :153 and :155 the fmt.Println diagnostics that were moved to stderr on 2026-09-06 because they corrupted the subagent JSON protocol and the ACP JSON-RPC stream. Git history: version-controlled since 2933dda5 and carried through the CONST-052 rename cc339fc0, so it is not a stray leftover. Inert today; the risk is that TestLoad_NoFmtPrintlnOnStdoutInSource scans only *.go, so if the shadow is ever restored the stdout defect returns with no guard firing. | Either (a) §11.4.124 investigate-then-remove in its OWN removal-only commit citing the git-history evidence above, or (b) if the shadow is kept deliberately, extend the source-scan guard to cover it so it cannot silently reintroduce the defect. Do not bundle either with unrelated work. | RED today: extend TestLoad_NoFmtPrintlnOnStdoutInSource to scan config.go.clean — it must report config.go.clean:153 and :155. Paired §1.1 mutation: restore a fmt.Println into the shadow and assert the guard FAILs; remove it and assert GREEN. **RESOLVED 2026-09-06 — options (a) AND (b), both landed.** §11.4.124 investigation (all facts by execution): the file was ADDED at `d2c01732` (2025-11-12, "Auto-commit") — the earlier "version-controlled since 2933dda5" was a `git log --follow` RENAME artifact (--follow walked config.go.clean back into config.go); `git log --all --diff-filter=A -- '*.clean'` names d2c01732, and `git ls-tree` confirms the path is ABSENT at 2933dda5, b80b1a38, 469ec408, 943f0342 and ecee7a96. That introducing commit was an indiscriminate sweep: it versioned a 39 MB build binary (`HelixCode/bin/helixcode`) in the same commit — the CONST-053 forbidden class, not an authored contribution. Its CONTENT was never edited again: `5fcc5a49` is a mode-only change (100644→100755, 0 insertions, 0 deletions) and `cc339fc0` the CONST-052 rename — ten months of zero evolution while config.go grew 392→2536 lines. NO CONSUMER EVER: `git log --all -S 'config.go.clean'` returns ZERO commits across all refs, so no build, script, Makefile, doc or tool has ever named it; in the live tree the only mention was this ledger. NO HIDDEN REFERENCE: it was the ONLY `*.clean` file in the entire tracked tree (so `.clean` is nobody's convention), no `//go:embed` in internal/config matches it, and no Makefile/script uses a `.clean` pattern. NOTHING LOST: 19 of its 20 top-level declarations exist in config.go, and the 20th — `setDefaults()` — has the live successor `setDefaultsOn(v *viper.Viper)` at `config.go:635` (the instance-scoped form that replaced the package-global viper style); every struct field it declares is present. Removed in removal-only commit `926eaf3e`. EXECUTED PROOF: the guard was extended FIRST (new `isScannedConfigSource` — a file is source if it ends `.go` OR contains `.go.`, i.e. keyed on the SHAPE, not a filename) and run against the tree as it stood → RED naming exactly `config.go.clean:153` and `config.go.clean:155`; GREEN after the removal. Paired §1.1 mutation planted a shadow under a DIFFERENT final extension (`config.go.bak`) → guard FAILed naming `config.go.bak:8`; restore sha256-verified identical for BOTH the guard file and the directory listing; residue scan clean. Because the predicate keys on shape, the whole class (`.go.bak`/`.orig`/`.old`/`.disabled`) is now closed, not just the one file. Guard: `internal/config/stdout_protocol_guard_test.go`. SIBLING FINDING (out of this stream's scope — REPORTED, NOT FIXED): three MORE tracked Go-source shadows exist elsewhere in the module — `internal/agent/types/planning_agent_test.go.bak2`, `internal/agent/types/planning_agent_test.go.bak3` and `internal/memory/providers/vector_provider_manager.go.bak2`. Each was checked: ZERO `fmt.Print*` occurrences, so they are NOT instances of THIS stdout defect — but they are the same never-compiled-shadow class, and both `.gitignore` files carry a `*.bak` rule (`.gitignore:39`, `helix_code/.gitignore:96`) that does NOT match a `.bak2`/`.bak3` suffix, so all three are TRACKED despite the intent to ignore them — a CONST-053 off-by-suffix hole. Their packages (`internal/agent`, `internal/memory/providers`) belong to other streams and were left untouched. The extended guard here is package-local to `internal/config` and does not see them. |
| HXC-002-F3-09 | helix_code | medium | `internal/server/wire_facade_live_e2e_test.go:268,433,468` | TestWireFacade_FullHTTP_E2E_LiveRoundTrip/tool_calls_shape_divergence_live asserts the served model emits a structured tool call (finish_reason "tool_calls" on the OpenAI wire, a tool_use block on the Anthropic wire) but pins HELIX_LLM_PROVIDER=helixllm (test:268), which routes to the llama.cpp CODER at :18434 — and the coder does not emit structured tool calls: it returns the call as a fenced/XML-ish blob in content with finish_reason "stop". Measured 2026-09-06 full-suite run: expected "tool_calls", actual "stop"; expected "tool_use", actual "end_turn". Corroborated by wire evidence captured BEFORE this batch: docs/qa/phase1_fullhttp_e2e_20260905T180015Z/04_openai_tool_calls_200.txt shows "finish_reason":"stop" from qwen2.5-coder-3b-instruct. The file is TRACKED and was NOT modified by this batch, so this is not a regression — it is a test asserting a capability the component under test does not have, which makes it permanently red and therefore useless as a signal. The sibling defect is the same root cause as the confirmed pre-existing TestToolCalling_ConcurrentLoad_ArgumentsAreTypedJSON failure. | The HelixLLM GATEWAY on :8443 DOES emit structured tool_calls for the same model — that is why HXC-002-F3-03 added the gateway route. Point this test at the gateway selector instead of pinning helixllm, so it asserts the capability against the surface that actually provides it. If a coder-route assertion is still wanted, split it into a separate case that asserts the coder's ACTUAL contract (fenced blob + finish_reason "stop") rather than a contract it does not implement, and SKIP-with-reason per §11.4.3 when the gateway is unreachable. | GREEN after re-pointing: the same assertions pass against the gateway route with the live gateway up, honest SKIP-with-reason when :8443 does not answer (never a faked PASS). Add a negative guard pinning the coder's real behaviour so a future coder that DOES gain tool_calls support fails loudly rather than silently changing the contract. Paired §1.1 mutation: point the gateway case back at the coder selector and assert the test FAILs. **RESOLVED 2026-09-07 — the recorded fix_direction was INCOMPLETE and is superseded.** That direction rested on a single-sample observation that the gateway emits structured tool_calls. MEASURED with 12 byte-identical POSTs to https://127.0.0.1:8443/v1/chat/completions (temperature 0, max_tokens 64, fixed prompt, fixed nonce): 8/12 finish_reason=tool_calls with tool_calls PRESENT, 4/12 finish_reason=length with tool_calls ABSENT — NON-DETERMINISTIC, two distinct outcomes for an identical request. Reproduced independently by running TestGatewayLive_ToolCallingCapabilityDivergence five times back to back: PASS, FAIL, PASS, PASS, PASS (the FAIL reporting expected "tool_calls" / actual "length"). An opt-in re-run at N=12 measured 10/12 vs 2/12 — same phenomenon, different split. So merely re-pointing the test at the gateway would have replaced a permanently-red test with a flaky one, which the operator's "all proofs MUST BE FULLY DETERMINISTIC" mandate forbids. LANDED INSTEAD — record-and-replay for the default verdict + an opt-in live distribution probe: (1) REAL wire responses were captured live from the gateway AND the coder into helix_code/testdata/toolcall_wire_corpus (21 bodies, sha256-pinned in provenance.json, regenerable with helix_code/testdata/toolcall_wire_corpus/capture.py) — including BOTH observed gateway branches, so the corpus records a non-deterministic backend honestly rather than snapshotting the convenient outcome; (2) those bytes are replayed through the UNMODIFIED production path — real curl over a real TCP socket, real gin router, real wireFacadeAuthMiddleware, real chatCompletions/anthropicMessages handlers, real resolveLLMProvider routing, real OpenAICompatibleProvider and real parseOpenAIWireToolCalls — by TestWireFacade_FullHTTP_E2E_ReplayToolCallShapes (internal/server/wire_facade_toolcall_replay_test.go), TestToolCallWireCorpus_CapabilityDivergence_Replay (internal/server/llm_generate_toolcall_replay_test.go) and TestToolCalling_ConcurrentReplay_ArgumentsAreTypedJSON (internal/llm/tool_calling_concurrent_replay_test.go); only the remote model process is replaced, by a recording of itself; (3) the live assertions are NOT deleted and NOT no-opped — they become opt-in DISTRIBUTION probes behind HELIX_LIVE_TOOLCALL_PROBE=1 that assert the CAPABILITY still exists (0/N structured tool calls FAILs as a contract change), so a gateway that lost tool-call support is still detectable. EXECUTED PROOF: BEFORE, 5 identical runs of the gateway guard gave PASS/FAIL/PASS/PASS/PASS, the wire-facade tool subtest FAILED 5/5 against the coder (expected "tool_calls"/actual "stop"; expected "tool_use"/actual "max_tokens"), and the D6 concurrent guard gave SKIP/FAIL/FAIL/FAIL/FAIL. AFTER, -count=10 on all five deterministic guards: 30/30 server PASS and 20/20 llm PASS with the live services UP, and byte-identically 30/30 and 20/20 with every endpoint env var pointed at a closed port — the verdict does not depend on the network or the model. PAIRED §1.1 MUTATIONS (both executed): mutating parseOpenAIWireToolCalls to return nil made both server replay guards FAIL ("must surface exactly one structured call from the recorded bytes; got 0"); mutating decodeAddArguments to accept everything made the D6 replay guard FAIL 16/16 as wrong-request matches and the analyzer self-validation FAIL on all four golden-bad fixtures; both restores sha256-verified identical, residue scan clean, GREEN re-verified. HONEST BOUNDARY (§11.4.6): the replay proves our parsing/routing/facades handle wire shapes the backends really produced at capture time; it cannot prove the backend still produces them today — that is exactly what the opt-in live probe is for, and neither claim is made stronger than its evidence. |
| HXC-002-F3-10 | helix_code | low | `scripts/gates/agent_config_fanout_gate.sh`, `scripts/install_agent_configs.sh`, `helix_code/internal/llm/factory_typed_nil_test.go` | Declared residual coverage gaps carried by the 2026-09-06 cloud-gate / CLI-agent-fanout batch. Each was identified and bounded by the agent that fixed the surrounding defect, and each is recorded here so a green guard is not mistaken for complete coverage. (1) GATE, scripts/gates/agent_config_fanout_gate.sh: a NO-ARG quoted array element `HELP=( "$ROOT/scripts/install_agent_configs.sh" )` is line-locally indistinguishable from a real no-arg invocation, so it still satisfies the command-position check; closing it needs array-context tracking, not a regex. (2) GATE SELF-TEST: of the 20 RED-A5 fixtures (4 forms x 5 path prefixes), only `array-literal` and `banner-plus-array` actually exercise the command-position regex — `continued-printf` is rejected because the joined line starts with `printf`, and `multiline-string` by the quote-state machine. A fixture rejected by a different mechanism than the one it names proves less than it appears to (this is the exact defect that made the pre-expansion RED-A5 a bluff gate). (3) REDACTION: the `\001` sentinel-forgery vector has NO permanent in-suite fixture, because json.dumps escapes a raw 0x01 to \u0001 so the JSON seed cannot carry the triggering byte; the live vector is the crush CLI-command text path, verified only by a standalone harness. (4) REDACTION: a single-character env-var key (e.g. "P") carries no credential word, so only value-shape rules can reach it. (5) TYPED-NIL STATIC SCAN, internal/llm/factory_typed_nil_test.go: not caught are a constructor reached through a variable or method value, a selector from another package, taint through a SECOND assignment (`q := p`), and returns inside nested function literals (deliberately not descended into). The dynamic sweep covers arms the scan cannot see, but the pair is not exhaustive. (6) GATE, three §11.4.201 false refusals that fail CLOSED (safe direction, but they would break setup.sh if it adopted those forms): a heredoc body line ending in `\` joins the terminator so the heredoc never ends; a call inside `$( )`; and `$'don\'t'` ANSI-C quoting desyncs the quote state. **CORRECTION (2026-09-06, §11.4.6) — two residuals recorded above are WRONG, disproved by measurement in the round-5 scripts review. (1) The gate residual was recorded as "line-locally indistinguishable, needs array-context tracking, not a regex". That overstates it in BOTH directions. Scope: not ONE shape but TEN fail open, including three never declared — `HELP=( ./scripts/x.sh --dry-run )` unquoted with args, `X=( ./scripts/x.sh )`, and a NEW class `X=$(echo ./scripts/x.sh --dry-run)` where `$(echo` is consumed as an assignment value — plus a heredoc terminator that awk `trim()`s and bash does not. Difficulty: the SINGLE-LINE form closes with one character-class change (`=([^\"'[:space:](]*\|\$\()`), and the MULTI-LINE form needs about three lines of array-depth counting in the stripper. The reviewer built both and measured 10 fail-opens -> 0 with no new false refusals, RED 37/37, real tree GREEN. (2) The `\001` residual was recorded as having NO possible in-suite fixture because json.dumps escapes a raw 0x01. Wrong: the seed can be written with `printf` or `open(...,'wb')`. The same report's claim that the sentinel is "unforgeable by construction" is also false — 0x02, 0x1f, and the VALID-JSON forms `\u200bapiKey` and `\u0001apiKey` all leak, and `\uXXXX` is exactly how the installer's own json.dumps renders a non-ASCII key. Both are being fixed rather than left standing. The lesson worth keeping: a residual inherited from the report of the agent that wrote the code carries that agent's blind spot with it — the difficulty estimate needs independent measurement exactly as much as the defect claim does.** | None of these is a live exploit; the correct action is to keep them VISIBLE, not to rush a fix that trades a bounded known gap for an unbounded new one — the recurring failure mode of this batch was that each round's fix created the next round's finding. Priority if picked up: (1) and (3) are the two that could actually let something through — (1) a gate PASS with no invocation, (3) a credential printed verbatim — so they rank above the rest. (2) and (5) are honesty-of-coverage items: the guards work, but claim more scope than they have. (4) and (6) are accepted trades already argued in-code. | Per gap: (1) a fixture asserting a no-arg quoted array element FAILs the gate — requires array-context tracking first, so the fixture is written RED and stays RED until that lands (§11.4.115), never silently dropped. (2) annotate each RED-A5 fixture with the mechanism that actually rejects it and assert that mechanism, so a fixture cannot pass for the wrong reason. (3) a fixture on the crush CLI-command path (not the JSON path) carrying a raw 0x01. (4) a value-shape probe row for a 1-char key. (5) fixture cases for each declared-uncovered form, asserted RED, so the boundary is machine-checked rather than prose. (6) leave as-is unless setup.sh adopts one of the forms; a regression fixture would pin behaviour nobody depends on. |
| HXC-002-F3-11 | helix_code | high | `internal/llm/openai_compatible_provider.go` (RedactEndpointForMessage, redactURLErrorsInChain) | ROUND-7 CONST-042. **(A) BLOCKER, fixed.** RedactEndpointForMessage masked the userinfo ONLY when `u.User.Password()` reported no password component, and otherwise delegated to `url.URL.Redacted()` — which masks the PASSWORD and PRESERVES THE USERNAME, exactly as net/http's `stripPassword` does. Any endpoint with a password component therefore printed its username in full at all five helixagent transport sites, both carriers, and in the cloud-gate refusal that reaches a 500 body. This is not an exotic shape: `curl -u sk_test_xxx:` is Stripe's own documented form, so the credential IS the username and the password is empty-but-present — precisely the branch that was delegated. MEASURED pre-fix: `"https://sk_test_4eC39HqLyjWDarjtT1zdp7dc:xxxxx@api.example.com/v1"` in the refusal text. **(B) WHY THE GUARD MISSED IT — third instance of one trap.** The `user_and_password` row named a TWO-component credential and asserted only that the PASSWORD was absent, never the username. A half-assertion is green for the wrong reason. Every shape row was swept for the same defect; four rows forbade only part of the credential they named, and rows whose username was `svc`/`user` were renamed to an assertable token so both components could actually be checked. **(C) FOURTH CARRIER, found BY that sweep and fixed.** Renaming exposed a genuinely un-redacted carrier: `redactURLErrorsInChain` rewrote `ue.URL` only, while net/http echoes the endpoint's SCHEME inside the WRAPPED cause (`unsupported protocol scheme %q`) — and for a scheme-less endpoint (`sk_test_xxx:@host/v1`) net/url reads the CREDENTIAL as the scheme. The credential rode out of every transport site with the masked URL sitting beside it. Fixed by scrubbing the userinfo components out of the cause text through a wrapper that preserves `Unwrap`, `Timeout()` and `Temporary()`. **(D) A regression this round introduced and closed.** That wrapper initially implemented only `Unwrap`; `url.Error.Timeout()` type-asserts on `e.Err` DIRECTLY rather than via `errors.As`, so `net.Error.Timeout()` silently flipped to false — and `internal/server/llm_generate.go` selects HTTP status codes off exactly that. Reproduced by a dedicated test, then closed by forwarding both methods. | Replace the WHOLE userinfo unconditionally (`u.User = url.User(mask)`), never delegate to `Redacted()`; scrub userinfo components from the `*url.Error` cause text as well as its URL; forward `Timeout`/`Temporary` through any wrapper placed in an `*url.Error.Err` slot. | RED captured on the pre-fix artifact at all three surfaces (24 failing assertions: 6 gate-site, 15 transport, 3 server-500). GREEN after. Paired §1.1 mutation reverting the masking to its conditional form: 45 CONST-042 violations across the three surfaces, byte-identical sha256 restore, zero residue. |
| HXC-002-F3-12 | helix_code | medium | `internal/llm/cloud_gate_koboldai_test.go`, `internal/llm/cloud_gate_redaction_scan_test.go`, `internal/llm/factory_typed_nil_test.go`, `internal/llm/openai_compatible_provider.go` | ROUND-7 guard-scope findings, all reproduced by execution before fixing. **(1) Exempt-type guard drove 2 of 12.** `TestNewProvider_ExemptOpenAICompatibleRefusedAtHostedEndpoint` asserted the downstream locality gate for `vllm` and `lmstudio` only, while `isCloudGateExemptProviderType`'s self-gating clause lists twelve — including `xiaomi`, whose DEFAULT endpoint is HOSTED (`https://api.xiaomimimo.com/v1`) and which was therefore the arm where a missing downstream gate mattered most. Same structural class as the half-assertion above: a guard whose scope is a hardcoded SUBSET of the real list. Now parses the clause out of `factory.go` and drives all twelve; adding a type to the switch without coverage FAILS. **(2) Redaction scanner blinded by the round-6 narrowing.** The N5 fix (judge the selector, then stop) left selectors judged against the ENDPOINT regex alone, so `cfg.APIKey` — which has no redaction helper at all — and `cfg.String()` both scored zero findings. Both now flagged; the real-tree scan still passes, so no false refusal was introduced. **(3) Error-chain depth was a silent fail-open at 33+.** Raised 32 -> 256 and, more importantly, BOTH facts are now machine-checked: a test proves the walk reaches the bound, and a second test PINS the residual beyond it so the declared limit cannot quietly become a lie. **(4) Typed-nil scan accepted a dead-sibling reset.** `nilResetPositions` inspected the whole guard subtree, so `if c == 999 { p = nil }` counted as a reset for every other path — a live typed-nil escape certified as correct. Now counts only DIRECT statements of the guard block. The SECOND declared gap (a guard with no return plus a trailing bare return outside it) is NOT closed; it is pinned as an explicit `DECLARED_GAP_` fixture so the boundary is asserted rather than described. | Bind guard scope to the production list it guards (parse it, never restate it); widen the scanner's selector rule to credential-named fields and config stringers; machine-check both sides of every declared bound. | All four reproduced RED and verified GREEN by name-filtered runs. `make verify-compile` GREEN. Full `./internal/llm/...` and `./internal/server`: the ONLY failures are the two pre-existing ones already recorded as HXC-002-F3-09 (`tool_calls_shape_divergence_live` and `TestToolCalling_ConcurrentLoad_ArgumentsAreTypedJSON`), both in files this batch did not modify. |


## JSON (same schema, exact keys)

```json
[
  {
    "id": "HXC-002-F3-01",
    "repo": "helix_code",
    "severity": "critical",
    "file": "internal/server/llm_generate.go:176-180",
    "defect": "Config llm.default_provider (\"local\", internal/config/config.go:676) is never consumed by the server generate path: SelectorInput.Config is hardcoded \"\" (llm_generate.go:179), so provider-less requests fall to Ollama :11434 (llm_generate.go:251-266) instead of the config-declared \"local\" route to the coder at :18434 (llm_generate.go:202-204,530).",
    "fix_direction": "Thread cfg.LLM.DefaultProvider into SelectorInput.Config (precedence Flag > Env > Config already honoured at internal/llm/provider_factory.go:57); \"local\" in config must resolve to the helixllm coder route, not Ollama.",
    "test_plan": "Unit RED->GREEN with RED_MODE polarity per §11.4.115: resolveLLMProvider(\"\",\"\") under config default \"local\" constructs a provider with BaseURL http://localhost:18434 (not :11434); live round-trip mirroring internal/server/llm_generate_helixllm_live_test.go:53 against coder /v1/models, honest SKIP-when-unreachable."
  },
  {
    "id": "HXC-002-F3-02",
    "repo": "helix_code",
    "severity": "critical",
    "file": "config/config.yaml:154-173",
    "defect": "Dead provider block helix-llm/helix-debate: unknown types (no case in internal/llm/provider_factory.go:83-113), zero Go references, wrong default endpoints (:8081 = HelixCode server itself, :8082 = ChromaDB per config/replica-8081.yaml:187-196). Whole llm.providers block discarded at load: LLMConfig lacks a Providers field (internal/config/config.go:197-226) and strict.go:315-317 reports it discarded.",
    "fix_direction": "Remove the verbatim block (Step 2 capture) from config/config.yaml; drop dead keys HELIX_LLM_ENDPOINT/HELIX_LLM_API_KEY from .env.example:44,50; if a gateway provider is wanted, add it as a real factory type (F3-03) instead of resurrecting this block.",
    "test_plan": "RED: startup strict-load no longer reports llm.providers discarded for these entries; grep gate: zero references to helix-llm/helix-debate outside the removal commit; config load test asserting no ErrorUnused for the removed block."
  },
  {
    "id": "HXC-002-F3-03",
    "repo": "helix_code",
    "severity": "high",
    "file": "internal/server/llm_generate.go:530",
    "defect": "No route to the HelixLLM gateway (https://127.0.0.1:8443, unit helixllm-gateway.service): zero code/config references to :8443 (only comment config/replica-8081.yaml:189). The helixllm/local selector is hardwired to the llama.cpp coder at http://localhost:18434, plain HTTP, no TLS config, no gateway env key, no API-key handling. Gateway live reachability UNCONFIRMED (read-only pass).",
    "fix_direction": "Add a gateway endpoint key (env HELIX_LLM_GATEWAY_ENDPOINT or config-injected per §11.4.28) plus a provider route building an HTTPS client against the gateway with TLS trust config; keep the :18434 coder route as direct-sidecar fallback.",
    "test_plan": "Unit: selector gateway/helixllm-gateway resolves BaseURL https://127.0.0.1:8443; live test GET /v1/models over TLS then completion round-trip, mirroring llm_generate_helixllm_live_test.go with §11.4.3 SKIP-when-unreachable (no fake PASS)."
  },
  {
    "id": "HXC-002-F3-04",
    "repo": "helix_code",
    "severity": "high",
    "file": "cmd/other_commands.go:116-134",
    "defect": "helix generate CLI is non-functional: NewModelManager() registers zero providers (internal/llm/model_manager.go:70-76) so SelectOptimalModel always errors \"no models available\" (model_manager.go:109-110) regardless of llm.default_provider; ProviderType(\"local\") resolves against an empty provider map. cfg.LLM.DefaultProvider is read but can never resolve to a provider instance.",
    "fix_direction": "Route helix generate through the same resolver semantics as the server (resolveLLMProvider) or register config providers into ModelManager before selection; map \"local\" to the coder/gateway route.",
    "test_plan": "RED: helix generate \"prompt\" with default config errors \"no models available\" (capture as defect-present evidence); GREEN: same command returns a real completion from the local route; §11.4.135 regression guard pinning provider registration so the empty-map defect cannot recur."
  },
  {
    "id": "HXC-002-F3-05",
    "repo": "helix_code",
    "severity": "medium",
    "file": "module-wide (zero matches for 'colibri')",
    "defect": "Colibri is entirely absent from the Go module (zero case-insensitive matches in .go/.yaml/.md/.sh); spec 002's dynamic hardware-based model selection has no implementation surface in helix_code. Closest existing machinery: ModelManager hardware detector hook (internal/llm/model_manager.go:70-72) with no Colibri policy on top. Presence in submodules outside helix_code/: UNCONFIRMED.",
    "fix_direction": "Locate/confirm the Colibri component (§11.4.74 catalogue check); define the hardware-profile -> model-tier selection contract as config-injected data; wire it between hardware detection and provider/model selection.",
    "test_plan": "Unit: hardware-profile -> model-selection table with boundary cases; integration: selection against the real coder catalog (/v1/models) asserting a verifier-sourced model per CONST-036; honest SKIP-with-reason when the Colibri service is absent."
  },
  {
    "id": "HXC-002-F3-06",
    "repo": "helix_code",
    "severity": "medium",
    "file": ".env.example:50",
    "defect": "Env-var contract drift: .env.example documents HELIX_LLAMA_CPP_HOST=http://localhost:8080, but :8080 is HelixCode's own server port (self-POST 404/502 measured and documented at internal/server/llm_generate.go:619-630); code default is :18434. Dead keys HELIX_LLM_ENDPOINT/HELIX_DEBATE_ENDPOINT/HELIX_DEBATE_API_KEY are read by no Go code.",
    "fix_direction": "Align .env.example with real defaults (:18434 coder, :11434 ollama), remove dead gateway/debate keys, and document the single override convention (HELIX_LLM_LOCAL_OPENAI_ENDPOINT).",
    "test_plan": "RED: loading .env.example defaults must never construct a provider pointing at :8080; unit asserting envLlamaCppHost() precedence chain (explicit env > HELIX_LLM_LOCAL_OPENAI_ENDPOINT > :18434) per internal/server/llm_generate_llamacpp_local_test.go:327.",
    "correction_note": "2026-09-05, section 11.4.6 accuracy correction: the :8080 claim above was already stale when this ledger was authored -- the :8080 to :18434 fix landed in commit a74ae7cb, an ancestor of this ledger's own commit 502b45d9. The residual real work -- the two genuinely-dead keys HELIX_LLM_API_KEY and HELIX_LLM_ENDPOINT (the HELIX_DEBATE_* keys were already absent from .env.example) -- has now been removed."
  },
  {
    "id": "HXC-002-F3-07",
    "repo": "helix_code",
    "severity": "medium",
    "file": "internal/providers/ai_integration.go:286-300",
    "defect": "The process-global cloud gate is enforced at llm.NewCloudProvider and (since 2026-09-06) at llm.NewProvider, but three constructor families bypass BOTH by calling the concrete hosted constructors directly: createAIProvider (internal/providers/ai_integration.go:286-300) dispatches to NewOpenAIProvider/NewAnthropicProvider/NewGeminiProvider with no gate check; litellm.NewUnifiedProvider (internal/llm/litellm/unified_provider.go:20) builds a bearer-auth HTTP client against an arbitrary endpoint; internal/llm/providers/cerebras/cerebras.go:77 likewise. VERIFIED LATENT, NOT LIVE (2026-09-06): createAIProvider is unexported, and package internal/providers is imported by exactly one non-test file (internal/i18nwiring/wire.go:169) which registers i18n translators and constructs no provider; litellm.NewUnifiedProvider has one in-package caller (litellm/registry.go:29) and is referenced only by internal/clarification, whose Engine nothing instantiates; cerebras has zero non-test constructor callers. No shipping path reaches an ungated cloud constructor today — but the gate is bypassed silently the moment internal/providers is wired in.",
    "fix_direction": "Prefer routing createAIProvider through the gated llm.NewProvider (small, testable, keeps the gate in one place). Gating inside each concrete hosted constructor gives higher assurance but has a large blast radius: dozens of existing tests construct NewAnthropicProvider/NewBedrockProvider directly and would all need §11.4.120 reconciliation in one change.",
    "test_plan": "RED: with the gate CLOSED, a guard driving createAIProvider (or its exported entry, once one exists) for a hosted ProviderType must refuse with an error wrapping ErrCloudDisabled — today it constructs. Plus a package-boundary gate asserting no non-test file outside internal/llm calls llm.New{OpenAI,Anthropic,Gemini,Bedrock,Cohere,Mistral}Provider directly; paired §1.1 mutation adds such a call and asserts the gate FAILs.",
    "resolution": "**PARTIALLY RESOLVED 2026-09-06 — the `internal/providers` half is CLOSED; the two `internal/llm` files are ASSESSED ONLY (owned by another stream, not edited).** LATENCY RE-VERIFIED INDEPENDENTLY by execution (not inherited): `internal/providers` has exactly ONE non-test importer, `internal/i18nwiring/wire.go:169`, which uses exactly one symbol from it (`providerspkg.SetTranslator` at `:530`) and constructs no provider; and `NewAIIntegration` — the sole route to `createAIProvider`, via `(*AIIntegration).Initialize:250` — has ZERO non-test callers. FIX CHOSEN — gate AT the dispatch, not routing through `llm.NewProvider`: routing would need an adapter between the two distinct interfaces (`providers.AIProvider` vs `llm.Provider`), whereas the in-place gate adds ZERO new imports because `ai_integration.go` already imports `internal/llm` (so no cycle risk) — the genuinely smaller and more testable change. GATED SET DETERMINED BY READING EVERY ARM'S BODY, not by which type names sound hosted: exactly three arms delegate to a concrete hosted constructor (`openai`→`llm.NewOpenAIProvider`, `anthropic`→`llm.NewAnthropicProvider`, `gemini`→`llm.NewGeminiProvider`); the other ten (cohere, huggingface, mistral, gemma, llamaindex, memgpt, crewai, characterai, replika, anima) return `newNotImplementedProvider` — they construct nothing and dial nothing, so gating them would break local routes for no safety gain. EXECUTED PROOF: RED pre-fix — all three hosted subtests got a nil error with the gate CLOSED, and the run log emitted `✅ OpenAI provider initialized with 4 models`, i.e. the bypass OBSERVED at runtime with credentials present, not inferred from source; GREEN post-fix; three paired §1.1 mutations each killed exactly its intended test and nothing else — delete the gate → the three hosted subtests FAIL; invert the predicate (gate everything) → the non-hosted positive control FAILs; ignore the gate state (always refuse hosted) → the gate-OPEN test FAILs — with every restore sha256-identical and a clean residue scan. `make verify-compile` (incl. test-code compile), `vet`, and the FULL `internal/config` + `internal/providers` packages all pass. Guard: `internal/providers/cloud_gate_dispatch_test.go`. AST-SCAN FEASIBILITY (measured answer to this entry's own proposal): a name-based ban on `llm.New{OpenAI,Anthropic,Gemini,Bedrock,Cohere,Mistral,...}Provider` outside `internal/llm` is NOT landable as specified. The measured population of such non-test call sites is exactly FOUR in TWO files: the three now-GATED sites in `ai_integration.go:1783,1813,1858` (which a name ban would flag even though they are correct) and `tests/integration/cmd/p1f15_challenge/main.go:337`, a non-test challenge binary outside this stream's scope. The correct form is gate-PROXIMITY AST analysis — \"is this call dominated, within its own function, by a `CloudEnabled()` / `ErrCloudDisabled` guard?\" — which IS feasible (`internal/llm/cloud_gate_redaction_scan_test.go`'s WalkDir+AST walk is the working template) but must land together with a decision on the p1f15 challenge site, so it is a SEPARATE work item, not a rider on this one. REMAINING OPEN (assessment for the owning stream, reachability verified read-only): (1) `internal/llm/litellm/unified_provider.go:20` — LATENT: `NewUnifiedProvider`'s only non-test caller is `(*Registry).Register` (`litellm/registry.go:29`), and `Registry.Register` / `litellm.NewRegistry` have ZERO non-test callers anywhere; the only non-test importers of the package are `internal/clarification/{engine,question}.go`, which take a `*litellm.UnifiedProvider` as a constructor PARAMETER and never build one, and `internal/clarification` itself has one non-test importer (`i18nwiring/wire.go:103`) using only `SetTranslator` (`:299`), while `clarification.NewEngine`'s only non-test caller is its sibling `NewQuestionGenerator` (`question.go:18`), itself uncalled. FIX SHAPE: unlike the named-vendor arms this one targets an ARBITRARY endpoint, so the predicate must be ENDPOINT-locality, not a type name — `internal/llm` already has exactly that in `isLocalEndpointURL` (provider_factory.go, the documented companion to `isLocalProviderType`); it is unexported today, so the owning stream either exports it or puts the check inside package `llm` and has litellm call through it. (2) `internal/llm/providers/cerebras/cerebras.go:77` — LATENT and the most isolated of the three: `cerebras.NewProvider` has ZERO non-test importers of its package (the only non-test textual match outside the file is a self-referential comment at `cerebras.go:59`) and ZERO non-test constructor calls. FIX SHAPE: Cerebras is an unambiguously hosted vendor, so it takes the simple form — a `!llm.CloudEnabled()` check at the top of `NewProvider` returning an error wrapping `llm.ErrCloudDisabled`; the package already imports `llm` (its signature takes `llm.ProviderConfigEntry`), so zero new imports and no cycle risk, the same property that made the `internal/providers` fix small."
  },
  {
    "id": "HXC-002-F3-08",
    "repo": "helix_code",
    "severity": "low",
    "file": "internal/config/config.go.clean:153,155",
    "defect": "internal/config/config.go.clean is a TRACKED but never-compiled shadow of config.go (the extension is not .go, so the toolchain never sees it). It still carries at :153 and :155 the fmt.Println diagnostics that were moved to stderr on 2026-09-06 because they corrupted the subagent JSON protocol and the ACP JSON-RPC stream. Git history: version-controlled since 2933dda5 and carried through the CONST-052 rename cc339fc0, so it is not a stray leftover. Inert today; the risk is that TestLoad_NoFmtPrintlnOnStdoutInSource scans only *.go, so if the shadow is ever restored the stdout defect returns with no guard firing.",
    "fix_direction": "Either (a) §11.4.124 investigate-then-remove in its OWN removal-only commit citing the git-history evidence above, or (b) if the shadow is kept deliberately, extend the source-scan guard to cover it so it cannot silently reintroduce the defect. Do not bundle either with unrelated work.",
    "test_plan": "RED today: extend TestLoad_NoFmtPrintlnOnStdoutInSource to scan config.go.clean — it must report config.go.clean:153 and :155. Paired §1.1 mutation: restore a fmt.Println into the shadow and assert the guard FAILs; remove it and assert GREEN.",
    "resolution": "**RESOLVED 2026-09-06 — options (a) AND (b), both landed.** §11.4.124 investigation (all facts by execution): the file was ADDED at `d2c01732` (2025-11-12, \"Auto-commit\") — the earlier \"version-controlled since 2933dda5\" was a `git log --follow` RENAME artifact (--follow walked config.go.clean back into config.go); `git log --all --diff-filter=A -- '*.clean'` names d2c01732, and `git ls-tree` confirms the path is ABSENT at 2933dda5, b80b1a38, 469ec408, 943f0342 and ecee7a96. That introducing commit was an indiscriminate sweep: it versioned a 39 MB build binary (`HelixCode/bin/helixcode`) in the same commit — the CONST-053 forbidden class, not an authored contribution. Its CONTENT was never edited again: `5fcc5a49` is a mode-only change (100644→100755, 0 insertions, 0 deletions) and `cc339fc0` the CONST-052 rename — ten months of zero evolution while config.go grew 392→2536 lines. NO CONSUMER EVER: `git log --all -S 'config.go.clean'` returns ZERO commits across all refs, so no build, script, Makefile, doc or tool has ever named it; in the live tree the only mention was this ledger. NO HIDDEN REFERENCE: it was the ONLY `*.clean` file in the entire tracked tree (so `.clean` is nobody's convention), no `//go:embed` in internal/config matches it, and no Makefile/script uses a `.clean` pattern. NOTHING LOST: 19 of its 20 top-level declarations exist in config.go, and the 20th — `setDefaults()` — has the live successor `setDefaultsOn(v *viper.Viper)` at `config.go:635` (the instance-scoped form that replaced the package-global viper style); every struct field it declares is present. Removed in removal-only commit `926eaf3e`. EXECUTED PROOF: the guard was extended FIRST (new `isScannedConfigSource` — a file is source if it ends `.go` OR contains `.go.`, i.e. keyed on the SHAPE, not a filename) and run against the tree as it stood → RED naming exactly `config.go.clean:153` and `config.go.clean:155`; GREEN after the removal. Paired §1.1 mutation planted a shadow under a DIFFERENT final extension (`config.go.bak`) → guard FAILed naming `config.go.bak:8`; restore sha256-verified identical for BOTH the guard file and the directory listing; residue scan clean. Because the predicate keys on shape, the whole class (`.go.bak`/`.orig`/`.old`/`.disabled`) is now closed, not just the one file. Guard: `internal/config/stdout_protocol_guard_test.go`. SIBLING FINDING (out of this stream's scope — REPORTED, NOT FIXED): three MORE tracked Go-source shadows exist elsewhere in the module — `internal/agent/types/planning_agent_test.go.bak2`, `internal/agent/types/planning_agent_test.go.bak3` and `internal/memory/providers/vector_provider_manager.go.bak2`. Each was checked: ZERO `fmt.Print*` occurrences, so they are NOT instances of THIS stdout defect — but they are the same never-compiled-shadow class, and both `.gitignore` files carry a `*.bak` rule (`.gitignore:39`, `helix_code/.gitignore:96`) that does NOT match a `.bak2`/`.bak3` suffix, so all three are TRACKED despite the intent to ignore them — a CONST-053 off-by-suffix hole. Their packages (`internal/agent`, `internal/memory/providers`) belong to other streams and were left untouched. The extended guard here is package-local to `internal/config` and does not see them."
  },
  {
    "id": "HXC-002-F3-09",
    "repo": "helix_code",
    "severity": "medium",
    "file": "internal/server/wire_facade_live_e2e_test.go:268,433,468",
    "defect": "TestWireFacade_FullHTTP_E2E_LiveRoundTrip/tool_calls_shape_divergence_live asserts the served model emits a structured tool call (finish_reason \"tool_calls\" on the OpenAI wire, a tool_use block on the Anthropic wire) but pins HELIX_LLM_PROVIDER=helixllm (test:268), which routes to the llama.cpp CODER at :18434 — and the coder does not emit structured tool calls: it returns the call as a fenced/XML-ish blob in content with finish_reason \"stop\". Measured 2026-09-06 full-suite run: expected \"tool_calls\", actual \"stop\"; expected \"tool_use\", actual \"end_turn\". Corroborated by wire evidence captured BEFORE this batch: docs/qa/phase1_fullhttp_e2e_20260905T180015Z/04_openai_tool_calls_200.txt shows \"finish_reason\":\"stop\" from qwen2.5-coder-3b-instruct. The file is TRACKED and was NOT modified by this batch, so this is not a regression — it is a test asserting a capability the component under test does not have, which makes it permanently red and therefore useless as a signal. The sibling defect is the same root cause as the confirmed pre-existing TestToolCalling_ConcurrentLoad_ArgumentsAreTypedJSON failure.",
    "fix_direction": "The HelixLLM GATEWAY on :8443 DOES emit structured tool_calls for the same model — that is why HXC-002-F3-03 added the gateway route. Point this test at the gateway selector instead of pinning helixllm, so it asserts the capability against the surface that actually provides it. If a coder-route assertion is still wanted, split it into a separate case that asserts the coder's ACTUAL contract (fenced blob + finish_reason \"stop\") rather than a contract it does not implement, and SKIP-with-reason per §11.4.3 when the gateway is unreachable.",
    "test_plan": "GREEN after re-pointing: the same assertions pass against the gateway route with the live gateway up, honest SKIP-with-reason when :8443 does not answer (never a faked PASS). Add a negative guard pinning the coder's real behaviour so a future coder that DOES gain tool_calls support fails loudly rather than silently changing the contract. Paired §1.1 mutation: point the gateway case back at the coder selector and assert the test FAILs.",
    "resolution": "**RESOLVED 2026-09-07 — the recorded fix_direction was INCOMPLETE and is superseded.** That direction rested on a single-sample observation that the gateway emits structured tool_calls. MEASURED with 12 byte-identical POSTs to https://127.0.0.1:8443/v1/chat/completions (temperature 0, max_tokens 64, fixed prompt, fixed nonce): 8/12 finish_reason=tool_calls with tool_calls PRESENT, 4/12 finish_reason=length with tool_calls ABSENT — NON-DETERMINISTIC, two distinct outcomes for an identical request. Reproduced independently by running TestGatewayLive_ToolCallingCapabilityDivergence five times back to back: PASS, FAIL, PASS, PASS, PASS (the FAIL reporting expected \"tool_calls\" / actual \"length\"). An opt-in re-run at N=12 measured 10/12 vs 2/12 — same phenomenon, different split. So merely re-pointing the test at the gateway would have replaced a permanently-red test with a flaky one, which the operator's \"all proofs MUST BE FULLY DETERMINISTIC\" mandate forbids. LANDED INSTEAD — record-and-replay for the default verdict + an opt-in live distribution probe: (1) REAL wire responses were captured live from the gateway AND the coder into helix_code/testdata/toolcall_wire_corpus (21 bodies, sha256-pinned in provenance.json, regenerable with helix_code/testdata/toolcall_wire_corpus/capture.py) — including BOTH observed gateway branches, so the corpus records a non-deterministic backend honestly rather than snapshotting the convenient outcome; (2) those bytes are replayed through the UNMODIFIED production path — real curl over a real TCP socket, real gin router, real wireFacadeAuthMiddleware, real chatCompletions/anthropicMessages handlers, real resolveLLMProvider routing, real OpenAICompatibleProvider and real parseOpenAIWireToolCalls — by TestWireFacade_FullHTTP_E2E_ReplayToolCallShapes (internal/server/wire_facade_toolcall_replay_test.go), TestToolCallWireCorpus_CapabilityDivergence_Replay (internal/server/llm_generate_toolcall_replay_test.go) and TestToolCalling_ConcurrentReplay_ArgumentsAreTypedJSON (internal/llm/tool_calling_concurrent_replay_test.go); only the remote model process is replaced, by a recording of itself; (3) the live assertions are NOT deleted and NOT no-opped — they become opt-in DISTRIBUTION probes behind HELIX_LIVE_TOOLCALL_PROBE=1 that assert the CAPABILITY still exists (0/N structured tool calls FAILs as a contract change), so a gateway that lost tool-call support is still detectable. EXECUTED PROOF: BEFORE, 5 identical runs of the gateway guard gave PASS/FAIL/PASS/PASS/PASS, the wire-facade tool subtest FAILED 5/5 against the coder (expected \"tool_calls\"/actual \"stop\"; expected \"tool_use\"/actual \"max_tokens\"), and the D6 concurrent guard gave SKIP/FAIL/FAIL/FAIL/FAIL. AFTER, -count=10 on all five deterministic guards: 30/30 server PASS and 20/20 llm PASS with the live services UP, and byte-identically 30/30 and 20/20 with every endpoint env var pointed at a closed port — the verdict does not depend on the network or the model. PAIRED §1.1 MUTATIONS (both executed): mutating parseOpenAIWireToolCalls to return nil made both server replay guards FAIL (\"must surface exactly one structured call from the recorded bytes; got 0\"); mutating decodeAddArguments to accept everything made the D6 replay guard FAIL 16/16 as wrong-request matches and the analyzer self-validation FAIL on all four golden-bad fixtures; both restores sha256-verified identical, residue scan clean, GREEN re-verified. HONEST BOUNDARY (§11.4.6): the replay proves our parsing/routing/facades handle wire shapes the backends really produced at capture time; it cannot prove the backend still produces them today — that is exactly what the opt-in live probe is for, and neither claim is made stronger than its evidence."
  },
  {
    "id": "HXC-002-F3-10",
    "repo": "helix_code",
    "severity": "low",
    "file": "scripts/gates/agent_config_fanout_gate.sh",
    "defect": "Declared residual coverage gaps carried by the 2026-09-06 cloud-gate / CLI-agent-fanout batch. Each was identified and bounded by the agent that fixed the surrounding defect, and each is recorded here so a green guard is not mistaken for complete coverage. (1) GATE, scripts/gates/agent_config_fanout_gate.sh: a NO-ARG quoted array element `HELP=( \"$ROOT/scripts/install_agent_configs.sh\" )` is line-locally indistinguishable from a real no-arg invocation, so it still satisfies the command-position check; closing it needs array-context tracking, not a regex. (2) GATE SELF-TEST: of the 20 RED-A5 fixtures (4 forms x 5 path prefixes), only `array-literal` and `banner-plus-array` actually exercise the command-position regex — `continued-printf` is rejected because the joined line starts with `printf`, and `multiline-string` by the quote-state machine. A fixture rejected by a different mechanism than the one it names proves less than it appears to (this is the exact defect that made the pre-expansion RED-A5 a bluff gate). (3) REDACTION: the `\\001` sentinel-forgery vector has NO permanent in-suite fixture, because json.dumps escapes a raw 0x01 to \\u0001 so the JSON seed cannot carry the triggering byte; the live vector is the crush CLI-command text path, verified only by a standalone harness. (4) REDACTION: a single-character env-var key (e.g. \"P\") carries no credential word, so only value-shape rules can reach it. (5) TYPED-NIL STATIC SCAN, internal/llm/factory_typed_nil_test.go: not caught are a constructor reached through a variable or method value, a selector from another package, taint through a SECOND assignment (`q := p`), and returns inside nested function literals (deliberately not descended into). The dynamic sweep covers arms the scan cannot see, but the pair is not exhaustive. (6) GATE, three §11.4.201 false refusals that fail CLOSED (safe direction, but they would break setup.sh if it adopted those forms): a heredoc body line ending in `\\` joins the terminator so the heredoc never ends; a call inside `$( )`; and `$'don\\'t'` ANSI-C quoting desyncs the quote state.",
    "fix_direction": "None of these is a live exploit; the correct action is to keep them VISIBLE, not to rush a fix that trades a bounded known gap for an unbounded new one — the recurring failure mode of this batch was that each round's fix created the next round's finding. Priority if picked up: (1) and (3) are the two that could actually let something through — (1) a gate PASS with no invocation, (3) a credential printed verbatim — so they rank above the rest. (2) and (5) are honesty-of-coverage items: the guards work, but claim more scope than they have. (4) and (6) are accepted trades already argued in-code.",
    "test_plan": "Per gap: (1) a fixture asserting a no-arg quoted array element FAILs the gate — requires array-context tracking first, so the fixture is written RED and stays RED until that lands (§11.4.115), never silently dropped. (2) annotate each RED-A5 fixture with the mechanism that actually rejects it and assert that mechanism, so a fixture cannot pass for the wrong reason. (3) a fixture on the crush CLI-command path (not the JSON path) carrying a raw 0x01. (4) a value-shape probe row for a 1-char key. (5) fixture cases for each declared-uncovered form, asserted RED, so the boundary is machine-checked rather than prose. (6) leave as-is unless setup.sh adopts one of the forms; a regression fixture would pin behaviour nobody depends on.",
    "correction_note": "CORRECTION (2026-09-06, §11.4.6) — two residuals recorded above are WRONG, disproved by measurement in the round-5 scripts review. (1) The gate residual was recorded as \"line-locally indistinguishable, needs array-context tracking, not a regex\". That overstates it in BOTH directions. Scope: not ONE shape but TEN fail open, including three never declared — `HELP=( ./scripts/x.sh --dry-run )` unquoted with args, `X=( ./scripts/x.sh )`, and a NEW class `X=$(echo ./scripts/x.sh --dry-run)` where `$(echo` is consumed as an assignment value — plus a heredoc terminator that awk `trim()`s and bash does not. Difficulty: the SINGLE-LINE form closes with one character-class change (`=([^\\\"'[:space:](]*|\\$\\()`), and the MULTI-LINE form needs about three lines of array-depth counting in the stripper. The reviewer built both and measured 10 fail-opens -> 0 with no new false refusals, RED 37/37, real tree GREEN. (2) The `\\001` residual was recorded as having NO possible in-suite fixture because json.dumps escapes a raw 0x01. Wrong: the seed can be written with `printf` or `open(...,'wb')`. The same report's claim that the sentinel is \"unforgeable by construction\" is also false — 0x02, 0x1f, and the VALID-JSON forms `\\u200bapiKey` and `\\u0001apiKey` all leak, and `\\uXXXX` is exactly how the installer's own json.dumps renders a non-ASCII key. Both are being fixed rather than left standing. The lesson worth keeping: a residual inherited from the report of the agent that wrote the code carries that agent's blind spot with it — the difficulty estimate needs independent measurement exactly as much as the defect claim does."
  },
  {
    "id": "HXC-002-F3-11",
    "repo": "helix_code",
    "severity": "high",
    "file": "internal/llm/openai_compatible_provider.go",
    "defect": "ROUND-7 CONST-042. RedactEndpointForMessage masked the userinfo ONLY when no password component existed and otherwise delegated to url.URL.Redacted(), which masks the PASSWORD and PRESERVES the USERNAME (as net/http stripPassword does). Any endpoint with a password component printed its username in full at five transport sites, both carriers, and in the gate refusal reaching a 500 body. Measured pre-fix: \"https://sk_test_4eC39HqLyjWDarjtT1zdp7dc:xxxxx@api.example.com/v1\". The vendor-documented `curl -u key:` form puts the credential in the username with an empty-but-present password, i.e. exactly the delegated branch. The guard missed it because the user_and_password row asserted only the password absent and never the username -- the third instance of that half-assertion trap in this batch. Sweeping every row for it exposed a FOURTH carrier: redactURLErrorsInChain rewrote ue.URL only, while net/http echoes the endpoint scheme in the wrapped cause (unsupported protocol scheme %q), and for a scheme-less endpoint net/url reads the CREDENTIAL as the scheme. Fixing that introduced and then closed a further regression: the scrub wrapper initially implemented only Unwrap, and url.Error.Timeout() type-asserts on e.Err directly, so net.Error.Timeout() silently flipped to false -- the signal internal/server/llm_generate.go selects HTTP status codes from.",
    "fix_direction": "Replace the WHOLE userinfo unconditionally; scrub userinfo components from the *url.Error cause text as well as its URL; forward Timeout/Temporary through any wrapper placed in an *url.Error.Err slot.",
    "test_plan": "RED captured on the pre-fix artifact at all three surfaces (24 failing assertions: 6 gate-site, 15 transport, 3 server-500); GREEN after. Paired section 1.1 mutation reverting the masking to its conditional form produced 45 CONST-042 violations across the three surfaces, with byte-identical sha256 restore and zero residue."
  },
  {
    "id": "HXC-002-F3-12",
    "repo": "helix_code",
    "severity": "medium",
    "file": "internal/llm/cloud_gate_koboldai_test.go",
    "defect": "ROUND-7 guard-scope findings, all reproduced by execution before fixing. (1) The exempt-type guard drove 2 of the 12 self-gating types in isCloudGateExemptProviderType, omitting xiaomi -- whose default endpoint is HOSTED and which was therefore the arm where a missing downstream gate mattered most. (2) The round-6 N5 narrowing left selectors judged against the ENDPOINT regex alone, so cfg.APIKey (which has no redaction helper at all) and cfg.String() both scored zero findings. (3) errorChainRedactionMaxDepth was a silent fail-open at 33+. (4) nilResetPositions inspected the whole guard subtree, so a reset in a dead sibling branch certified a live typed-nil escape as correct.",
    "fix_direction": "Bind guard scope to the production list it guards by parsing it rather than restating it; widen the scanner selector rule to credential-named fields and config stringers; machine-check both sides of every declared bound.",
    "test_plan": "All four reproduced RED and verified GREEN by name-filtered runs. The exempt list is now parsed from factory.go so adding a type without coverage FAILS. Depth has one test proving the walk reaches the bound and a second PINNING the residual beyond it. The second typed-nil gap is NOT closed; it is pinned as an explicit DECLARED_GAP_ fixture. make verify-compile GREEN; the only suite failures are the two pre-existing ones recorded as HXC-002-F3-09."
  }
]
```
